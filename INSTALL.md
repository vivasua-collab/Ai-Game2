# 📦 Установка и запуск Cultivation World Simulator

> Полное руководство по установке, настройке и запуску проекта.
> Последнее обновление: 2026

---

## 📋 Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| **Bun** | 1.0+ | 1.1+ |
| **Node.js** | 18+ | 20+ (если не используется Bun) |
| **RAM** | 4 GB | 8 GB |
| **Диск** | 500 MB | 1 GB |
| **ОС** | Linux, macOS, Windows (WSL2) | Linux, macOS |

---

## 🚀 Быстрый старт

### 1. Установка Bun (рекомендуется)

```bash
# macOS / Linux / WSL
curl -fsSL https://bun.sh/install | bash

# Windows (PowerShell)
powershell -c "irm bun.sh/install.ps1 | iex"

# Проверка
bun --version
```

### 2. Клонирование проекта

```bash
# Клонирование
git clone https://github.com/vivasua-collab/Ai-Game2.git

# Переход в директорию
cd Ai-Game2

# Переключение на актуальную ветку
git checkout master3
```

### 3. Установка зависимостей

```bash
# Установка всех зависимостей
bun install
```

### 4. Инициализация базы данных

```bash
# Генерация Prisma клиента
bun run db:generate

# Применение схемы к БД
bun run db:push
```

### 5. Запуск

```bash
# Режим разработки
bun run dev

# Открыть в браузере
# http://localhost:3000
```

---

## 🔧 Подробная установка

### Вариант A: Bun (рекомендуется)

```bash
# 1. Установка Bun
curl -fsSL https://bun.sh/install | bash
source ~/.bashrc  # или ~/.zshrc

# 2. Клонирование
git clone https://github.com/vivasua-collab/Ai-Game2.git
cd Ai-Game2
git checkout master3

# 3. Установка зависимостей
bun install

# 4. База данных
bun run db:generate
bun run db:push

# 5. Запуск
bun run dev
```

### Вариант B: Node.js + npm

```bash
# 1. Клонирование
git clone https://github.com/vivasua-collab/Ai-Game2.git
cd Ai-Game2
git checkout master3

# 2. Установка зависимостей
npm install

# 3. База данных
npm run db:generate
npm run db:push

# 4. Запуск
npm run dev
```

---

## 📁 Структура проекта

```
Ai-Game2/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API эндпоинты
│   │   ├── page.tsx           # Главная страница
│   │   └── layout.tsx         # Корневой layout
│   │
│   ├── components/            # React компоненты
│   │   ├── game/             # Игровые компоненты
│   │   ├── start/            # Стартовый экран
│   │   └── ui/               # shadcn/ui компоненты
│   │
│   ├── data/                  # Данные и пресеты
│   │   ├── presets/          # Пресеты (техники, навыки, персонажи)
│   │   ├── prompts/          # Промпты для LLM
│   │   └── cultivation-levels.ts
│   │
│   ├── hooks/                 # React хуки
│   │   ├── useGame.ts        # Основной хук игры
│   │   └── use-toast.ts      # Уведомления
│   │
│   ├── lib/                   # Библиотеки
│   │   ├── game/             # Игровая логика
│   │   ├── llm/              # LLM интеграция
│   │   ├── db.ts             # Prisma клиент
│   │   └── logger/           # Логирование
│   │
│   ├── services/              # Сервисный слой
│   │   ├── game.service.ts
│   │   ├── character.service.ts
│   │   ├── world.service.ts
│   │   └── session.service.ts
│   │
│   ├── stores/                # Zustand хранилища
│   │   └── game.store.ts
│   │
│   └── types/                 # TypeScript типы
│       └── game.ts
│
├── prisma/
│   └── schema.prisma          # Схема БД (v4)
│
├── db/
│   ├── custom.db             # SQLite база данных
│   └── backups/              # Резервные копии
│
├── docs/
│   └── DECOMPOSITION_PLAN.md  # План развития
│
├── .env                       # Переменные окружения
├── INSTALL.md                 # Этот файл
├── ARCHITECTURE.md            # Архитектура проекта
└── README.md                  # Общее описание
```

---

## 🛠️ Доступные скрипты

| Скрипт | Описание |
|--------|----------|
| `bun run dev` | Запуск сервера разработки (порт 3000) |
| `bun run build` | Сборка для продакшена |
| `bun start` | Запуск продакшен сервера |
| `bun run lint` | Проверка ESLint |
| `bun run db:push` | Применить схему Prisma к БД |
| `bun run db:generate` | Генерация Prisma клиента |
| `bun run db:migrate` | Создание миграции |
| `bun run db:reset` | Сброс базы данных |
| `bun run db:studio` | Открыть Prisma Studio |

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# База данных
DATABASE_URL="file:./db/custom.db"

# LLM (опционально)
LLM_PROVIDER="z-ai"
LLM_API_KEY="your-api-key"
```

### Настройка LLM

Проект поддерживает несколько провайдеров LLM:

1. **z-ai** (по умолчанию) - через z-ai-web-dev-sdk
2. **local** - локальный Ollama
3. **api** - кастомный API

---

## 🗄️ База данных

### Схема (версия 4)

- **Character** - персонажи с навыками культивации
- **Technique** - техники (с уровнем, требованиями)
- **CharacterTechnique** - изученные техники (прогресс, мастерство)
- **Location** - локации (3D координаты)
- **NPC** - неигровые персонажи
- **Sect** - секты
- **GameSession** - игровые сессии
- **SystemLog** - системные логи

### Сброс БД

```bash
# Полный сброс
bun run db:reset

# Или вручную
rm -f db/custom.db
bun run db:push
```

---

## 🐛 Устранение неполадок

### Ошибка: "Cannot find module"

```bash
# Переустановка зависимостей
rm -rf node_modules
bun install
```

### Ошибка: "Database is locked"

```bash
# Остановка всех процессов
pkill -f "next"

# Перезапуск
bun run db:push
bun run dev
```

### Ошибка: "Prisma Client not generated"

```bash
bun run db:generate
```

### Порт 3000 занят

```bash
# Найти процесс
lsof -i :3000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
PORT=3001 bun run dev
```

---

## 📱 Режимы запуска

### Разработка

```bash
bun run dev
# - Горячая перезагрузка
# - Подробные ошибки
# - DevTools
```

### Продакшен

```bash
# Сборка
bun run build

# Запуск
bun start
# - Оптимизированная сборка
# - Минимум логов
# - Лучшая производительность
```

---

## 🔄 Обновление проекта

```bash
# Получить последние изменения
git fetch origin
git pull origin master3

# Обновить зависимости
bun install

# Обновить БД при необходимости
bun run db:push
```

---

## 🧪 Проверка установки

После успешной установки:

1. Откройте http://localhost:3000
2. Выберите вариант старта (секта/свобода/кастомный)
3. Введите имя персонажа
4. Начните игру!

---

## 📞 Поддержка

При проблемах с установкой:

1. Проверьте версию Bun: `bun --version`
2. Проверьте .env файл
3. Проверьте логи: `tail -f db/system.log`
4. Сбросьте БД и попробуйте снова

---

*Документация актуальна на 2026 год. Версия схемы БД: 4*
