# Database Configuration
# SQLite database file path
# For Windows: use absolute path like file:C:/path/to/project/db/custom.db
DATABASE_URL="file:./db/custom.db"

# =====================================================
# LLM Provider Configuration
# =====================================================
# Priority: Ollama (local, free) -> Z-AI (requires config) -> API
# =====================================================

# OPTION 1: Ollama (RECOMMENDED - FREE, LOCAL)
# 1. Install: https://ollama.com/download
# 2. Run: ollama pull llama3
# 3. Ollama starts automatically - no config needed!

# OPTION 2: Z-AI SDK (requires API key)
# Create .z-ai-config file in project root:
# {
#   "baseUrl": "https://your-api-server.com/v1",
#   "apiKey": "YOUR_API_KEY"
# }

# OPTION 3: External API (OpenAI, etc.)
# LLM_PROVIDER=api
# LLM_API_KEY=sk-your-api-key
# LLM_API_ENDPOINT=https://api.openai.com/v1/chat/completions
# LLM_API_MODEL=gpt-4

# Logging level: ERROR, WARN, INFO, DEBUG
LOG_LEVEL=INFO
