# 🎯 Чекпоинт 28: Рефакторинг системы генератора техник

**Версия:** 1.3  
**Дата:** 2026-02-28  
**Статус:** ✅ Завершён (включая исправления багов)

---

## ⚠️ Важное уточнение

**Генератор техник — обособленный проект с минимальной интеграцией в игру.**

- Файлы в `presets/` используются игрой в тестовом режиме
- В будущем планируется полное обособление (статус: ⏸️ На паузе)
- Интеграция в игру НЕ требуется — только улучшение самого генератора
- См. `docs/PROJECT_ROADMAP.md` → "Обособление хранилища техник"

---

## ✅ Выполнено

### 1. Система категорий оружия
**Файл:** `src/lib/generator/weapon-categories.ts`

- 6 категорий оружия вместо 12 конкретных типов
- Категории: `one_handed_blade`, `one_handed_blunt`, `two_handed_heavy`, `two_handed_polearm`, `light_fist`, `exotic`
- Бонусы для каждого типа оружия в категории
- Особые эффекты (backstab, stun, cleave, bleed, crush, bind, pierce, etc.)

### 2. Новые ID префиксы
**Файл:** `src/lib/generator/id-config.ts`

| Старый префикс | Новые префиксы | Описание |
|---------------|----------------|----------|
| TC | MS | Melee Strike — удар телом |
| TC | MW | Melee Weapon — оружейная техника |
| TC | RG | Ranged — дальняя атака |

### 3. Новая структура файлов
**Файл:** `src/lib/generator/preset-storage.ts`

```
presets/techniques/
  combat/
    melee-strike/level-1.json   (MS_)
    melee-weapon/level-1.json   (MW_)
    ranged/level-1.json         (RG_)
  defense/level-1.json          (DF_)
  healing/level-1.json          (HL_)
```

### 4. Избирательная очистка
**API:** `POST /api/generator/techniques`

```typescript
// Очистить всё
{ action: 'clear', scope: 'all' }

// Очистить по типу
{ action: 'clear', scope: 'type', targetType: 'defense' }

// Очистить по подтипу combat
{ action: 'clear', scope: 'subtype', targetSubtype: 'melee_weapon' }
```

### 5. UI изменения
**Файлы:** 
- `src/components/settings/TechniqueGeneratorPanel.tsx`
- `src/components/settings/SettingsPanel.tsx`

- Динамическая кнопка очистки (показывает текущий тип/подтип)
- Выбор категории оружия вместо конкретного типа
- Отображение бонусов категории и оружия

### 6. Интеграция категорий в генератор
**Файл:** `src/lib/generator/technique-generator.ts`

- `generateMeleeWeaponTechnique()` переписан для работы с категориями
- Выбор: категория → конкретное оружие из категории
- Применение бонусов: `damageMod`, `speedMod`, `rangeMod`, `critBonus`
- Особые эффекты автоматически добавляются в модификаторы техники
- Новое поле `weaponCategory` в интерфейсе `BaseTechnique`

---

## 🔧 Исправления багов (v1.3)

### 7. Исправление TYPE_ID_PREFIX
**Файл:** `src/lib/generator/technique-generator.ts`
**Проблема:** `TYPE_ID_PREFIX['combat']` использовал устаревший префикс `'TC'`
**Решение:** Заменён на `'MS'` (базовый префикс для combat)

### 8. Исправление NaN% эффективности
**Файл:** `src/lib/game/techniques.ts`
**Проблема:** `calculateTechniqueEffectiveness()` возвращал `NaN` при undefined полях в `statScaling`
**Решение:** Добавлены проверки на null/undefined и защита от NaN

### 9. Обновление счётчиков
**Файл:** `presets/counters.json`
**Изменения:**
- Удалён устаревший префикс `TC`
- Добавлены новые префиксы: `MS`, `MW`, `RG` (инициализированы нулями)

---

## 📊 Статистика изменений

| Категория | Создано | Изменено |
|-----------|---------|----------|
| Новые файлы | 2 | - |
| Переписанные файлы | - | 5 |
| Строк кода | ~700 | ~900 |

---

## 📝 Что дальше (не включено в эту задачу)

1. **Миграция данных** — скрипт для пересохранения существующих техник в новой структуре
2. **Тестирование** — генерация техник каждого типа с категориями
3. **Интеграция в игру** — использование weaponCategory при применении техник

---

## 📚 Связанные документы

- [checkpoint28-solutions.md](./checkpoint28-solutions.md) — варианты решений
- [preset-storage.md](./docs/preset-storage.md) — система хранения
- [PROJECT_ROADMAP.md](./docs/PROJECT_ROADMAP.md) — задача "Обособление хранилища техник"
