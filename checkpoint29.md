# 📍 Чекпоинт 29 - Система генераторов предметов

**Дата:** 2026-03-01
**Ветка:** main2d3
**Статус:** 📝 Планирование

---

## 📋 Обзор

Разработка системы генераторов для всех типов предметов:
1. **Оружие** — мечи, копья, посохи, луки, кинжалы, кулаки
2. **Экипировка** — броня (голова, торс, ноги, ступни, спина)
3. **Аксессуары** — кольца, амулеты, талисманы
4. **Расходные материалы** — таблетки, эликсиры, еда, свитки
5. **Материалы** — руды, травы, кристаллы, эссенции
6. **Камни Ци** — кристаллизованная энергия (уже есть заготовка)
7. **Зарядники** — устройства для поглощения Ци из камней

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     СИСТЕМА ГЕНЕРАТОРОВ ПРЕДМЕТОВ                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     SETTINGS PANEL (React)                       │   │
│   │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │   │
│   │  │ Техники  │ │Экипировка│ │   NPC    │ │ Формации │            │   │
│   │  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │   │
│   │  ┌─────────────────────────────────────────────────────┐        │   │
│   │  │  ЭКИПИРОВКА (вкладка)                               │        │   │
│   │  │  ├── Оружие (подвкладка)                            │        │   │
│   │  │  ├── Броня (подвкладка)                             │        │   │
│   │  │  ├── Аксессуары (подвкладка)                        │        │   │
│   │  │  ├── Расходники (подвкладка)                        │        │   │
│   │  │  ├── Материалы (подвкладка)                         │        │   │
│   │  │  ├── Камни Ци (подвкладка)                          │        │   │
│   │  │  └── Зарядники (подвкладка)                         │        │   │
│   │  └─────────────────────────────────────────────────────┘        │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              │                                           │
│                              │ API: /api/generator/items                 │
│                              ▼                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     GENERATORS (Backend)                         │   │
│   │                                                                  │   │
│   │  src/lib/generator/                                             │   │
│   │  ├── technique-generator.ts  (существует)                       │   │
│   │  ├── weapon-generator.ts     (НОВОЕ)                            │   │
│   │  ├── armor-generator.ts      (НОВОЕ)                            │   │
│   │  ├── accessory-generator.ts  (НОВОЕ)                            │   │
│   │  ├── consumable-generator.ts (НОВОЕ)                            │   │
│   │  ├── material-generator.ts   (НОВОЕ)                            │   │
│   │  ├── qi-stone-generator.ts   (НОВОЕ)                            │   │
│   │  ├── charger-generator.ts    (НОВОЕ)                            │   │
│   │  ├── item-config.ts          (НОВОЕ - конфигурация)             │   │
│   │  ├── item-categories.ts      (НОВОЕ - категории)                │   │
│   │  └── preset-storage.ts       (расширен)                         │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                              │                                           │
│                              ▼                                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                     STORAGE (presets/)                           │   │
│   │                                                                  │   │
│   │  presets/                                                       │   │
│   │  ├── items/                                                     │   │
│   │  │   ├── weapons/                                               │   │
│   │  │   │   ├── one-handed-blade/level-{n}.json                   │   │
│   │  │   │   ├── one-handed-blunt/level-{n}.json                   │   │
│   │  │   │   ├── two-handed-heavy/level-{n}.json                   │   │
│   │  │   │   └── ...                                                │   │
│   │  │   ├── armor/                                                 │   │
│   │  │   │   ├── head/level-{n}.json                               │   │
│   │  │   │   ├── torso/level-{n}.json                              │   │
│   │  │   │   └── ...                                                │   │
│   │  │   ├── accessories/level-{n}.json                            │   │
│   │  │   ├── consumables/level-{n}.json                            │   │
│   │  │   ├── materials/level-{n}.json                              │   │
│   │  │   ├── qi-stones/level-{n}.json                              │   │
│   │  │   └── chargers/level-{n}.json                               │   │
│   │  └── counters.json (расширен)                                  │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🆔 Система ID для предметов

### Новые ID префиксы

| Префикс | Полное название | Категория | Пример |
|---------|-----------------|-----------|--------|
| **WP** | Weapon | Оружие | WP_000042 |
| **AR** | Armor | Броня | AR_000123 |
| **AC** | Accessory | Аксессуар | AC_000007 |
| **CS** | Consumable | Расходник | CS_000512 |
| **MT** | Material | Материал | MT_000089 |
| **QS** | Qi Stone | Камень Ци | QS_000033 |
| **CH** | Charger | Зарядник | CH_000015 |

### Под-префиксы (опционально, для детализации)

```
WP_SWD — Sword (меч)
WP_SPR — Spear (копьё)
WP_STF — Staff (посох)
WP_BOW — Bow (лук)
WP_DAG — Dagger (кинжал)
WP_FST — Fist (кулаки)

AR_HED — Head (голова)
AR_TRS — Torso (торс)
AR_LEG — Legs (ноги)
AR_FET — Feet (ступни)
AR_BAK — Back (спина)

AC RNG — Ring (кольцо)
AC_AML — Amulet (амулет)
AC_TAL — Talisman (талисман)

CS_PIL — Pill (таблетка)
CS_ELX — Elixir (эликсир)
CS_FOD — Food (еда)
CS_SCR — Scroll (свиток)

MT_ORE — Ore (руда)
MT_HRB — Herb (трава)
MT_CRY — Crystal (кристалл)
MT_ESS — Essence (эссенция)
```

---

## 📊 Детальный план генераторов

### 1. Генератор оружия (weapon-generator.ts)

**Типы оружия (по категориям):**

| Категория | Типы | Базовая дальность | Особенности |
|-----------|------|-------------------|-------------|
| `one_handed_blade` | sword, blade, saber, dagger | 1.0-1.5м | Скорость, крит |
| `one_handed_blunt` | mace, hammer, club | 0.8-1.2м | Оглушение, бронебой |
| `two_handed_heavy` | greatsword, greataxe, maul | 1.5-2.0м | Урон, медленный |
| `two_handed_polearm` | spear, halberd, glaive, staff | 2.0-3.0м | Дальность, контроль |
| `light_fist` | fist, claw, knuckle | 0.5м | Скорость, комбо |
| `exotic` | whip, chain, fan | 1.0-2.5м | Особые эффекты |

**Параметры генерации:**

```typescript
interface WeaponGenerationOptions {
  category?: WeaponCategory;
  weaponType?: WeaponType;
  level?: number;           // 1-9, уровень силы
  rarity?: ItemRarity;      // common → mythic
  element?: Element;        // Стихия (опционально)
  count?: number;
  mode: 'replace' | 'append';
  
  // Границы параметров
  damageBounds?: { min: number; max: number };
  speedBounds?: { min: number; max: number };
  rangeBounds?: { min: number; max: number };
}

interface GeneratedWeapon extends InventoryItem {
  // Идентификация
  id: string;               // WP_NNNNNN
  category: WeaponCategory;
  weaponType: WeaponType;
  
  // Боевые характеристики
  damage: {
    min: number;
    max: number;
    type: 'physical' | 'elemental';
    element?: Element;
  };
  
  speed: number;            // Атак в секунду
  range: number;            // Метры
  criticalChance: number;   // %
  criticalMultiplier: number;
  
  // Требования
  requirements: {
    level: number;
    strength?: number;
    agility?: number;
    intelligence?: number;
  };
  
  // Особые эффекты
  specialEffects: WeaponEffect[];
  
  // Мета
  meta: {
    seed: number;
    generatedAt: string;
    generatorVersion: string;
  };
}
```

**Формулы расчёта:**

```typescript
// Базовый урон = level × baseMultiplier × rarityMult
const BASE_DAMAGE_BY_LEVEL = {
  1: 5, 2: 10, 3: 20, 4: 35, 5: 55,
  6: 85, 7: 125, 8: 180, 9: 250
};

// Множитель редкости
const RARITY_DAMAGE_MULT = {
  common: 0.8, uncommon: 1.0, rare: 1.25, epic: 1.6, legendary: 2.0, mythic: 2.5
};

// Множитель категории
const CATEGORY_DAMAGE_MULT = {
  one_handed_blade: 1.0,
  one_handed_blunt: 1.1,
  two_handed_heavy: 1.4,
  two_handed_polearm: 1.2,
  light_fist: 0.7,
  exotic: 1.0
};
```

---

### 2. Генератор брони (armor-generator.ts)

**Слоты брони:**

| Слот | Название | Защита | Вес | Особенности |
|------|----------|--------|-----|-------------|
| `head` | Голова | 5-15% | 1-3 кг | Защита критов |
| `torso` | Торс | 15-35% | 5-15 кг | Основная защита |
| `legs` | Ноги | 10-25% | 3-8 кг | Мобильность |
| `feet` | Ступни | 5-15% | 1-3 кг | Скорость |
| `back` | Спина | 5-20% | 1-5 кг | Особые эффекты |

**Параметры генерации:**

```typescript
interface ArmorGenerationOptions {
  slot?: ArmorSlot;
  armorType?: 'light' | 'medium' | 'heavy';
  level?: number;
  rarity?: ItemRarity;
  element?: Element;        // Сопротивление стихии
  count?: number;
  mode: 'replace' | 'append';
}

interface GeneratedArmor extends InventoryItem {
  id: string;               // AR_NNNNNN
  slot: ArmorSlot;
  armorType: 'light' | 'medium' | 'heavy';
  
  // Защита
  defense: {
    physical: number;       // % снижения физ. урона
    elemental?: {           // Сопротивления стихиям
      fire?: number;
      water?: number;
      earth?: number;
      air?: number;
      lightning?: number;
      void?: number;
    };
  };
  
  // Характеристики
  stats: {
    weight: number;         // кг
    moveSpeedPenalty: number; // %
    fatigueReduction?: number;
  };
  
  // Бонусы
  bonuses?: {
    qiRegen?: number;
    healthRegen?: number;
    cultivationSpeed?: number;
  };
}
```

---

### 3. Генератор аксессуаров (accessory-generator.ts)

**Типы аксессуаров:**

| Тип | Слот | Основные бонусы |
|-----|------|-----------------|
| `ring` | Рука | Характеристики, крит |
| `amulet` | Шея | Защита, HP, Qi |
| `talisman` | Пояс | Особые эффекты, скиллы |

**Параметры генерации:**

```typescript
interface AccessoryGenerationOptions {
  type?: 'ring' | 'amulet' | 'talisman';
  level?: number;
  rarity?: ItemRarity;
  element?: Element;
  count?: number;
  mode: 'replace' | 'append';
}

interface GeneratedAccessory extends InventoryItem {
  id: string;               // AC_NNNNNN
  accessoryType: 'ring' | 'amulet' | 'talisman';
  
  // Бонусы к характеристикам
  statBonus: {
    strength?: number;
    agility?: number;
    intelligence?: number;
    conductivity?: number;
    maxQi?: number;
    maxHealth?: number;
  };
  
  // Особые эффекты
  specialEffect?: {
    type: string;           // 'life_steal', 'qi_on_kill', etc.
    value: number;
    trigger: string;
  };
}
```

---

### 4. Генератор расходников (consumable-generator.ts)

**Типы расходников:**

| Тип | Эффекты | Длительность |
|-----|---------|--------------|
| `pill` | Лечение, Qi, баффы статов | Мгновенно / минуты |
| `elixir` | Сильные баффы | Минуты / часы |
| `food` | Регенерация | Минуты |
| `scroll` | Одноразовые эффекты | Мгновенно |

**Параметры генерации:**

```typescript
interface ConsumableGenerationOptions {
  type?: 'pill' | 'elixir' | 'food' | 'scroll';
  level?: number;
  rarity?: ItemRarity;
  primaryEffect?: 'heal' | 'qi' | 'buff' | 'cure';
  count?: number;
  mode: 'replace' | 'append';
}

interface GeneratedConsumable extends InventoryItem {
  id: string;               // CS_NNNNNN
  consumableType: 'pill' | 'elixir' | 'food' | 'scroll';
  
  // Эффекты
  effects: ConsumableEffect[];
  
  // Использование
  uses: number;             // 1 для большинства
  cooldown?: number;        // ТИКов
  
  // Требования
  requirements?: {
    level?: number;
  };
}

interface ConsumableEffect {
  type: 'heal' | 'qi' | 'buff' | 'cure' | 'stat_boost';
  value: number;
  duration?: number;        // ТИКов (для баффов)
  stat?: string;            // Для stat_boost
}
```

**Базовые значения:**

```typescript
const PILL_EFFECTS_BY_LEVEL = {
  1: { heal: 20, qi: 15 },
  2: { heal: 40, qi: 30 },
  3: { heal: 80, qi: 60 },
  4: { heal: 150, qi: 120 },
  5: { heal: 300, qi: 250 },
  6: { heal: 600, qi: 500 },
  7: { heal: 1200, qi: 1000 },
  8: { heal: 2500, qi: 2000 },
  9: { heal: 5000, qi: 4000 },
};
```

---

### 5. Генератор материалов (material-generator.ts)

**Типы материалов:**

| Тип | Назначение | Редкость |
|-----|------------|----------|
| `ore` | Кузнечное дело | common → epic |
| `herb` | Алхимия | common → rare |
| `crystal` | Зачарование | rare → legendary |
| `essence` | Культивация | epic → mythic |

**Параметры генерации:**

```typescript
interface MaterialGenerationOptions {
  type?: 'ore' | 'herb' | 'crystal' | 'essence';
  level?: number;
  rarity?: ItemRarity;
  count?: number;
  mode: 'replace' | 'append';
}

interface GeneratedMaterial extends InventoryItem {
  id: string;               // MT_NNNNNN
  materialType: 'ore' | 'herb' | 'crystal' | 'essence';
  
  // Свойства
  properties: {
    purity: number;         // % чистоты
    qiContent?: number;     // Для кристаллов и эссенций
    element?: Element;      // Стихийная принадлежность
  };
  
  // Использование
  usage: {
    crafting: string[];     // Рецепты
    cultivation?: boolean;  // Можно использовать в культивации
  };
}
```

---

### 6. Генератор камней Ци (qi-stone-generator.ts)

> Основано на документации docs/qi_stone.md

**Классификация:**

| Класс | Объём (см³) | Ци (ед) | Пример |
|-------|-------------|---------|--------|
| `dust` | < 0.1 | < 102 | Пыль Ци |
| `fragment` | 0.1-1 | 102-1,024 | Осколок |
| `small` | 1-8 | 1,024-8,192 | Малый камень |
| `medium` | 8-27 | 8,192-27,648 | Средний |
| `large` | 27-64 | 27,648-65,536 | Большой |
| `huge` | 64-125 | 65,536-128,000 | Огромный |
| `boulder` | > 125 | > 128,000 | Глыба |

**Качество:**

| Качество | Чистота | Эффективность |
|----------|---------|---------------|
| `raw` | 0-60% | 50% |
| `rough` | 60-70% | 65% |
| `refined` | 70-85% | 80% |
| `pure` | 85-95% | 92% |
| `flawless` | 95-100% | 100% |

**Параметры генерации:**

```typescript
interface QiStoneGenerationOptions {
  sizeClass?: QiStoneSize;
  quality?: QiStoneQuality;
  type?: 'calm' | 'chaotic';
  count?: number;
  mode: 'replace' | 'append';
}

interface GeneratedQiStone extends InventoryItem {
  id: string;               // QS_NNNNNN
  
  // Размер
  size: {
    side: number;           // см
    volume: number;         // см³
    surface: number;        // см²
    sizeClass: QiStoneSize;
  };
  
  // Тип и качество
  stoneType: 'calm' | 'chaotic';
  quality: QiStoneQuality;
  
  // Содержание Ци
  qiContent: {
    total: number;          // ед Ци = 1024 × объём
    current: number;        // После частичного использования
  };
  
  // Свойства
  properties: {
    releaseRate: number;    // ед/сек
    efficiency: number;     // % поглощения
  };
  
  // Состояние
  state: {
    isSealed: boolean;
    sealStrength?: number;
  };
}
```

---

### 7. Генератор зарядников (charger-generator.ts)

> **Зарядник** — устройство для контролируемого поглощения Ци из камней

**Типы зарядников:**

| Тип | Скорость | Эффективность | Особенности |
|-----|----------|---------------|-------------|
| `basic` | 1.5x | 100% | Простой кристалл |
| `advanced` | 2.0x | 100% | Авто-контроль |
| `master` | 3.0x | 100% | Фильтрация хаотичной Ци |
| `legendary` | 5.0x | 110% | Бонус к поглощению |

**Параметры генерации:**

```typescript
interface ChargerGenerationOptions {
  type?: 'basic' | 'advanced' | 'master' | 'legendary';
  level?: number;
  rarity?: ItemRarity;
  count?: number;
  mode: 'replace' | 'append';
}

interface GeneratedCharger extends InventoryItem {
  id: string;               // CH_NNNNNN
  chargerType: 'basic' | 'advanced' | 'master' | 'legendary';
  
  // Характеристики
  stats: {
    speedMultiplier: number;    // Множитель скорости поглощения
    efficiencyBonus: number;    // % бонус к эффективности
    qiBufferCapacity: number;   // Ёмкость буфера (ед Ци)
    slotCount: number;          // Слоты для камней
  };
  
  // Возможности
  capabilities: {
    autoRegulation: boolean;    // Автоматический контроль скорости
    chaoticFilter: boolean;     // Фильтрация хаотичной Ци
    multiStone: boolean;        // Несколько камней одновременно
  };
  
  // Требования
  requirements: {
    cultivationLevel: number;
    conductivity: number;
  };
}
```

---

## 📁 Структура файлов

### Новые файлы

```
src/lib/generator/
├── item-config.ts           # Конфигурация ID префиксов для предметов
├── item-categories.ts       # Категории предметов
├── weapon-generator.ts      # Генератор оружия
├── armor-generator.ts       # Генератор брони
├── accessory-generator.ts   # Генератор аксессуаров
├── consumable-generator.ts  # Генератор расходников
├── material-generator.ts    # Генератор материалов
├── qi-stone-generator.ts    # Генератор камней Ци
├── charger-generator.ts     # Генератор зарядников
└── preset-storage.ts        # (расширен для предметов)

src/components/settings/
├── EquipmentGeneratorPanel.tsx   # Панель генерации экипировки
├── WeaponGeneratorTab.tsx       # Вкладка оружия
├── ArmorGeneratorTab.tsx        # Вкладка брони
├── AccessoryGeneratorTab.tsx    # Вкладка аксессуаров
├── ConsumableGeneratorTab.tsx   # Вкладка расходников
├── MaterialGeneratorTab.tsx     # Вкладка материалов
├── QiStoneGeneratorTab.tsx      # Вкладка камней Ци
└── ChargerGeneratorTab.tsx      # Вкладка зарядников

src/app/api/generator/
└── items/
    └── route.ts             # API для генерации предметов
```

### Структура хранения

```
presets/
├── items/
│   ├── weapons/
│   │   ├── one-handed-blade/
│   │   │   └── level-{n}.json
│   │   ├── one-handed-blunt/
│   │   ├── two-handed-heavy/
│   │   ├── two-handed-polearm/
│   │   ├── light-fist/
│   │   └── exotic/
│   ├── armor/
│   │   ├── head/
│   │   ├── torso/
│   │   ├── legs/
│   │   ├── feet/
│   │   └── back/
│   ├── accessories/
│   │   └── level-{n}.json
│   ├── consumables/
│   │   └── level-{n}.json
│   ├── materials/
│   │   └── level-{n}.json
│   ├── qi-stones/
│   │   └── level-{n}.json
│   └── chargers/
│       └── level-{n}.json
├── manifest.json
└── counters.json
```

---

## 📅 План реализации

### Фаза 1: Базовая инфраструктура (2 дня)

| Задача | Приоритет | Статус |
|--------|-----------|--------|
| 1.1 Создать item-config.ts с ID префиксами | 🔴 Высокий | 📝 Запланировано |
| 1.2 Создать item-categories.ts с категориями | 🔴 Высокий | 📝 Запланировано |
| 1.3 Расширить preset-storage.ts для предметов | 🔴 Высокий | 📝 Запланировано |
| 1.4 Обновить docs/id-system.md | 🔴 Высокий | 📝 Запланировано |

### Фаза 2: Генератор оружия (3 дня)

| Задача | Приоритет | Статус |
|--------|-----------|--------|
| 2.1 weapon-generator.ts — базовая структура | 🔴 Высокий | 📝 Запланировано |
| 2.2 Формулы расчёта урона, скорости, дальности | 🔴 Высокий | 📝 Запланировано |
| 2.3 Особые эффекты оружия | 🟡 Средний | 📝 Запланировано |
| 2.4 WeaponGeneratorTab.tsx — UI | 🔴 Высокий | 📝 Запланировано |

### Фаза 3: Генератор брони (2 дня)

| Задача | Приоритет | Статус |
|--------|-----------|--------|
| 3.1 armor-generator.ts | 🔴 Высокий | 📝 Запланировано |
| 3.2 Формулы защиты и веса | 🔴 Высокий | 📝 Запланировано |
| 3.3 ArmorGeneratorTab.tsx — UI | 🔴 Высокий | 📝 Запланировано |

### Фаза 4: Остальные генераторы (4 дня)

| Задача | Приоритет | Статус |
|--------|-----------|--------|
| 4.1 accessory-generator.ts | 🟡 Средний | 📝 Запланировано |
| 4.2 consumable-generator.ts | 🟡 Средний | 📝 Запланировано |
| 4.3 material-generator.ts | 🟢 Низкий | 📝 Запланировано |
| 4.4 qi-stone-generator.ts | 🟡 Средний | 📝 Запланировано |
| 4.5 charger-generator.ts | 🟡 Средний | 📝 Запланировано |

### Фаза 5: Интеграция UI (2 дня)

| Задача | Приоритет | Статус |
|--------|-----------|--------|
| 5.1 EquipmentGeneratorPanel.tsx | 🔴 Высокий | 📝 Запланировано |
| 5.2 Интеграция в SettingsPanel.tsx | 🔴 Высокий | 📝 Запланировано |
| 5.3 API /api/generator/items | 🔴 Высокий | 📝 Запланировано |

---

## 💡 ИИ-предложения (опциональные улучшения)

### 1. Унифицированный BaseItemGenerator

> **Предложение:** Создать абстрактный базовый класс для всех генераторов с общими методами:
> - `generateId()` — генерация ID
> - `selectRarity()` — выбор редкости
> - `computeFinalValues()` — расчёт финальных параметров
> - `save()` — сохранение в preset-storage

```typescript
abstract class BaseItemGenerator<T extends InventoryItem> {
  protected idService: IdService;
  protected storage: PresetStorageService;
  
  abstract generate(options: GenerationOptions): Promise<T[]>;
  abstract getPrefix(): IdPrefix;
  
  protected selectRarity(rng: () => number, forced?: Rarity): Rarity { ... }
  protected generateId(): string { ... }
  async save(items: T[], mode: 'replace' | 'append'): Promise<void> { ... }
}
```

### 2. Система сетов (Item Sets)

> **Предложение:** Добавить систему сетов экипировки с бонусами за комплект:
> - 2 предмета: малый бонус
> - 4 предмета: средний бонус
> - 6 предметов: большой бонус

```typescript
interface ItemSet {
  id: string;
  name: string;
  items: string[];           // ID предметов сета
  bonuses: {
    pieces: number;
    effect: ItemEffect;
  }[];
}
```

### 3. Система улучшений (Upgrade System)

> **Предложение:** Добавить возможность улучшения предметов:
> - +1 уровень улучшения = +10% базовых характеристик
> - Максимум улучшений зависит от редкости
> - Требует материалы

```typescript
interface UpgradeResult {
  success: boolean;
  newLevel: number;
  materialsConsumed: Material[];
  statChanges: Record<string, number>;
}
```

### 4. Генератор имён

> **Предложение:** Создать единый генератор имён для всех типов предметов:
> - Префиксы по стихиям (Огненный, Ледяной...)
> - Префиксы по качеству (Великий, Древний...)
> - Суффиксы по эффектам (Убийцы, Дракона...)
> - Базовые имена по типу (Меч, Топор...)

---

## 🔗 Связанная документация

- [docs/id-system.md](./docs/id-system.md) — Система ID
- [docs/qi_stone.md](./docs/qi_stone.md) — Камни Ци
- [docs/equip.md](./docs/equip.md) — Система экипировки
- [docs/inventory-system.md](./docs/inventory-system.md) — Инвентарь
- [checkpoint28.md](./checkpoint28.md) — Предыдущий чекпоинт

---

*Документ создан: 2026-03-01*
*Версия: 1.0*
