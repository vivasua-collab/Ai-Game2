# ⚔️ Система боевых техник

**Версия:** 2.0  
**Создано:** 2026-02-12  
**Обновлено:** 2026-02-12  
**Статус:** Актуально

---

## 📋 Обзор

Боевые техники делятся на три основные группы:
1. **Ближний бой (Melee)** — контактные техники и усиление оружия
2. **Дальний бой (Ranged)** — дистанционные техники с падением урона
3. **Защитные (Defense)** — блок, уклонение, щиты

### Важный принцип: Наполнение техник

**У техник нет отката (cooldown).** Вместо этого работает система **наполнения Ци**:

```
┌─────────────────────────────────────────────────────────────────────┐
│                     МЕХАНИКА ИСПОЛЬЗОВАНИЯ ТЕХНИКИ                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   Команда → Накопление Ци → Техника готова → Применение             │
│             (время зависит                                          │
│              от проводимости)                                        │
│                                                                      │
│   ┌──────────┐    ┌──────────────┐    ┌──────────┐    ┌──────────┐ │
│   │ Активация │ -> │ Наполнение   │ -> │ Порог    │ -> │ Мгновенное│ │
│   │ техники  │    │ Ци из ядра  │    │ достигнут│    │ применение│ │
│   └──────────┘    └──────────────┘    └──────────┘    └──────────┘ │
│                         │                                            │
│                         ▼                                            │
│              Скорость = проводимость ед/сек                          │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

**Это делает классический ближний бой с оружием актуальным:**
- Технику нужно "зарядить" Ци перед применением
- Время зарядки зависит от проводимости меридиан
- Обычные удары оружием можно наносить мгновенно
- Тактика: чередовать обычные атаки и техники

---

## ⏱️ Система скорости каста

### Формула времени наполнения

```
время_наполнения = qiCost / проводимость (секунды)

Примеры:
- qiCost = 20, проводимость = 2.0 ед/сек → 10 секунд
- qiCost = 50, проводимость = 5.0 ед/сек → 10 секунд
- qiCost = 100, проводимость = 10.0 ед/сек → 10 секунд
```

### Таблица времени наполнения

| Стоимость Ци | Проводимость 1.0 | Проводимость 2.0 | Проводимость 5.0 | Проводимость 10.0 |
|--------------|------------------|------------------|------------------|-------------------|
| 5 Ци | 5 сек | 2.5 сек | 1 сек | 0.5 сек |
| 15 Ци | 15 сек | 7.5 сек | 3 сек | 1.5 сек |
| 30 Ци | 30 сек | 15 сек | 6 сек | 3 сек |
| 50 Ци | 50 сек | 25 сек | 10 сек | 5 сек |
| 100 Ци | 100 сек | 50 сек | 20 сек | 10 сек |

### Модификаторы скорости

```typescript
interface CastSpeedModifiers {
  // Базовая скорость = проводимость
  baseSpeed: number;  // ед/сек
  
  // Бонус от мастерства техники
  masteryBonus: number;  // -1% времени за 1% мастерства
  
  // Бонус от уровня культивации
  cultivationBonus: number;  // +5% скорости за уровень
  
  // Итоговая скорость
  effectiveSpeed: number;  // ед/сек с бонусами
}

// Формула:
effectiveSpeed = baseSpeed * (1 + cultivationLevel * 0.05) * (1 + mastery * 0.01)
```

---

## 🎯 Типы боевых техник

### Полный перечень

```typescript
type CombatTechniqueType = 
  // === БЛИЖНИЙ БОЙ ===
  | "melee_strike"       // Контактный удар (без оружия)
  | "melee_weapon"       // Усиление оружия / удар с оружием
  // === ДАЛЬНИЙ БОЙ ===
  | "ranged_projectile"  // Снаряд
  | "ranged_beam"        // Луч
  | "ranged_aoe"         // Область
  // === ЗАЩИТНЫЕ ===
  | "defense_block"      // Блок (снижение урона)
  | "defense_shield"     // Энергетический щит (поглощение)
  | "defense_dodge";     // Уклонение (реакция)
```

---

## 📊 Масштабирование от характеристик

### Ближний бой без оружия (melee_strike)

```
Урон = базовый_урон × (1 + (сила - 10) × 0.05 + (ловкость - 10) × 0.025)

Коэффициенты:
- Сила: 5% за единицу выше 10
- Ловкость: 2.5% за единицу выше 10 (половина от силы)
```

### Ближний бой с оружием (melee_weapon)

```
Урон = базовый_урон × (1 + (сила - 10) × 0.025 + (ловкость - 10) × 0.05)

Коэффициенты:
- Сила: 2.5% за единицу выше 10 (половина от ловкости)
- Ловкость: 5% за единицу выше 10

Причина: с оружием ловкость важнее силы (техника владения)
```

### Дальний бой (ranged_*)

```
Урон = базовый_урон × (1 + (ловкость - 10) × 0.025 + (интеллект - 10) × 0.05)

Коэффициенты:
- Ловкость: 2.5% за единицу выше 10 (точность)
- Интеллект: 5% за единицу выше 10 (контроль Ци)
```

### Защитные техники (defense_*)

```
Эффект = базовый_эффект × (1 + (интеллект - 10) × 0.05)

Коэффициенты:
- Интеллект: 5% за единицу выше 10 (понимание структуры Ци)
```

### Сводная таблица коэффициентов

| Тип техники | Сила | Ловкость | Интеллект | Проводимость |
|-------------|------|----------|-----------|--------------|
| melee_strike | **5%** | 2.5% | — | влияет на скорость |
| melee_weapon | 2.5% | **5%** | — | влияет на скорость |
| ranged_* | — | 2.5% | **5%** | влияет на скорость |
| defense_* | — | — | **5%** | влияет на скорость |

---

## 🛡️ Защитные техники

### 3.1 Блок (defense_block)

Активное снижение входящего урона.

**Характеристики:**
- `combatType: "defense_block"`
- Требует оружие или щит
- Снижает урон на процент
- Действует 1 ход или пока не будет пробит

**Параметры:**
```typescript
interface DefenseBlockEffects {
  combatType: "defense_block";
  damageReduction: number;   // Снижение урона (базовый %)
  blockChance: number;       // Шанс успешного блока (%)
  durability: number;        // "Прочность" блока (сколько урона выдержит)
  counterAttack?: number;    // Шанс контратаки (%)
}
```

**Примеры:**
- "Стойка черепахи" — +50% снижения урона, но -50% скорости
- "Железная стена" — блокирует 80% урона, выдерживает 100 ед.
- "Зеркальный блок" — 30% шанс отразить атаку

### 3.2 Энергетический щит (defense_shield)

Поглощение урона за счёт Ци.

**Характеристики:**
- `combatType: "defense_shield"`
- Создаёт барьер из Ци
- Поглощает урон до исчерпания заряда
- Можно перезаряжать

**Параметры:**
```typescript
interface DefenseShieldEffects {
  combatType: "defense_shield";
  shieldHP: number;          // "Здоровье" щита (в ед. урона)
  regeneration: number;      // Регенерация щита за ход (ед.)
  qiDrainPerHit: number;     // Расход Ци при попадании
  duration: number;          // Длительность (ходы или минуты)
}
```

**Примеры:**
- "Водяной барьер" — 50 HP щита, регенерация 5/ход
- "Ментальный щит" — защита от ментальных атак
- "Земляная броня" — 100 HP, не регенерирует

### 3.3 Уклонение (defense_dodge)

Реактивное избегание атак.

**Характеристики:**
- `combatType: "defense_dodge"`
- Увеличивает шанс уклонения
- Может дать бонус к контратаке
- Тратит выносливость

**Параметры:**
```typescript
interface DefenseDodgeEffects {
  combatType: "defense_dodge";
  dodgeChance: number;       // Добавочный шанс уклонения (%)
  counterBonus: number;      // Бонус к контратаке после уклонения (%)
  fatigueCost: number;       // Стоимость в усталости (%)
  duration: number;          // Длительность эффекта (ходы)
}
```

**Примеры:**
- "Тень призрака" — +30% уклонения на 3 хода
- "Поток воды" — уклонение + последующая контратака +20%
- "Мгновенный шаг" — 100% уклонение от одной атаки

---

## 📐 Обновлённые формулы расчёта

### calculateStatScaling() — новая версия

```typescript
function calculateStatScaling(
  character: Character,
  combatType: CombatTechniqueType
): number {
  const str = character.strength;
  const agi = character.agility;
  const intl = character.intelligence;
  
  const strBonus = Math.max(0, str - 10);
  const agiBonus = Math.max(0, agi - 10);
  const intlBonus = Math.max(0, intl - 10);
  
  let multiplier = 1.0;
  
  switch (combatType) {
    case "melee_strike":
      // Сила × 5% + Ловкость × 2.5%
      multiplier += strBonus * 0.05;
      multiplier += agiBonus * 0.025;
      break;
      
    case "melee_weapon":
      // Сила × 2.5% + Ловкость × 5%
      multiplier += strBonus * 0.025;
      multiplier += agiBonus * 0.05;
      break;
      
    case "ranged_projectile":
    case "ranged_beam":
    case "ranged_aoe":
      // Ловкость × 2.5% + Интеллект × 5%
      multiplier += agiBonus * 0.025;
      multiplier += intlBonus * 0.05;
      break;
      
    case "defense_block":
    case "defense_shield":
    case "defense_dodge":
      // Интеллект × 5%
      multiplier += intlBonus * 0.05;
      break;
  }
  
  return multiplier;
}
```

### calculateCastTime() — новая функция

```typescript
function calculateCastTime(
  qiCost: number,
  conductivity: number,
  cultivationLevel: number = 1,
  mastery: number = 0
): number {
  // Базовая скорость = проводимость ед/сек
  let effectiveSpeed = conductivity;
  
  // Бонус от уровня культивации (+5% за уровень)
  effectiveSpeed *= 1 + (cultivationLevel - 1) * 0.05;
  
  // Бонус от мастерства (+1% за 1% мастерства)
  effectiveSpeed *= 1 + mastery * 0.01;
  
  // Время наполнения
  const castTime = qiCost / effectiveSpeed;
  
  // Минимум 0.1 секунды
  return Math.max(0.1, castTime);
}
```

### calculateBlockResult()

```typescript
function calculateBlockResult(
  technique: Technique,
  character: Character,
  incomingDamage: number,
  attackerPenetration: number = 0
): BlockResult {
  
  const effects = technique.effects;
  
  // Базовое снижение урона
  let reduction = effects.damageReduction || 30;
  
  // Масштабирование от интеллекта
  const intBonus = Math.max(0, character.intelligence - 10) * 0.05;
  reduction *= (1 + intBonus);
  
  // Пробитие атакующего
  reduction *= (1 - attackerPenetration / 100);
  
  // Ограничение: максимум 90%
  reduction = Math.min(90, reduction);
  
  // Итоговый урон
  const blockedDamage = Math.floor(incomingDamage * reduction / 100);
  const finalDamage = incomingDamage - blockedDamage;
  
  // Проверка прочности блока
  const durability = effects.durability || 100;
  const blockHolds = blockedDamage < durability;
  
  return {
    blockedDamage,
    finalDamage,
    reduction,
    blockHolds,
    counterChance: effects.counterAttack || 0
  };
}
```

---

## 🎮 Примеры техник

### Защитные техники

#### Стойка черепахи (Defense Block)
```typescript
{
  id: "turtle_stance",
  name: "Стойка черепахи",
  techniqueType: "combat",
  element: "earth",
  level: 1,
  qiCost: 10,
  fatigueCost: { physical: 3, mental: 1 },
  effects: {
    combatType: "defense_block",
    damageReduction: 50,      // 50% базового снижения
    blockChance: 80,          // 80% шанс блока
    durability: 50,           // Выдерживает 50 урона
    duration: 1               // 1 ход
  }
}
```

#### Водяной барьер (Defense Shield)
```typescript
{
  id: "water_barrier",
  name: "Водяной барьер",
  techniqueType: "combat",
  element: "water",
  level: 2,
  qiCost: 25,
  fatigueCost: { physical: 1, mental: 4 },
  effects: {
    combatType: "defense_shield",
    shieldHP: 50,             // 50 единиц поглощения
    regeneration: 5,          // +5 за ход
    qiDrainPerHit: 3,         // -3 Ци при попадании
    duration: 5               // 5 минут
  }
}
```

#### Тень призрака (Defense Dodge)
```typescript
{
  id: "ghost_shadow",
  name: "Тень призрака",
  techniqueType: "combat",
  element: "air",
  level: 2,
  qiCost: 15,
  fatigueCost: { physical: 5, mental: 2 },
  effects: {
    combatType: "defense_dodge",
    dodgeChance: 30,          // +30% уклонения
    counterBonus: 15,         // +15% к контратаке
    fatigueCost: 10,          // 10% усталости при использовании
    duration: 3               // 3 хода
  }
}
```

---

## 📊 Таблица времени наполнения по типам

| Тип техники | Типичный qiCost | При проводимости 1.0 | При проводимости 5.0 |
|-------------|-----------------|----------------------|----------------------|
| melee_strike | 5-15 | 5-15 сек | 1-3 сек |
| melee_weapon | 10-25 | 10-25 сек | 2-5 сек |
| ranged_projectile | 15-30 | 15-30 сек | 3-6 сек |
| ranged_beam | 25-50 | 25-50 сек | 5-10 сек |
| ranged_aoe | 30-60 | 30-60 сек | 6-12 сек |
| defense_block | 5-15 | 5-15 сек | 1-3 сек |
| defense_shield | 15-35 | 15-35 сек | 3-7 сек |
| defense_dodge | 10-20 | 10-20 сек | 2-4 сек |

---

## 🎯 Баланс: Почему оружие актуально

### Сравнение: Техника vs Оружие

| Параметр | Обычная атака оружием | Техника |
|----------|----------------------|---------|
| Время применения | Мгновенно | Требует наполнения Ци |
| Урон | Базовый оружия | Базовый × множители |
| Затраты | Только выносливость | Ци + выносливость |
| Элемент | Нет (физический) | Может быть элементальным |
| Дальность | Ближняя | Зависит от типа |
| Сложность | Нет | Требует обучения |

### Тактический вывод

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ТАКТИКА БОЯ                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   1. Начало боя → Активация защитной техники (если есть время)      │
│                                                                      │
│   2. В процессе боя:                                                 │
│      - Обычные атаки оружием (мгновенно, нет затрат Ци)             │
│      - Начать наполнение техники → ждать → применить                │
│      - Уклонение от атак противника                                 │
│                                                                      │
│   3. Высокая проводимость → техники быстрее → чаще используются     │
│                                                                      │
│   4. Низкая проводимость → упор на оружие и защиту                  │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📁 Интерфейсы (обновлённые)

```typescript
interface CombatTechniqueEffects {
  // === ТИП (обязательно) ===
  combatType: CombatTechniqueType;
  
  // === УРОН (для атакующих) ===
  damage?: number;
  
  // === ДАЛЬНОСТЬ (для ranged) ===
  range?: {
    fullDamage: number;
    halfDamage: number;
    max: number;
  };
  
  // === КОНТАКТ ===
  contactRequired?: boolean;
  
  // === ОБЛАСТЬ ===
  aoeRadius?: number;
  
  // === ЗАЩИТА (для defense_*) ===
  damageReduction?: number;   // Снижение урона (%)
  blockChance?: number;       // Шанс блока (%)
  durability?: number;        // Прочность блока
  shieldHP?: number;          // Здоровье щита
  regeneration?: number;      // Регенерация щита/ход
  dodgeChance?: number;       // Шанс уклонения (%)
  counterBonus?: number;      // Бонус к контратаке (%)
  
  // === ЭЛЕМЕНТАЛЬНЫЕ ЭФФЕКТЫ ===
  elementalEffect?: ElementalEffect;
  
  // === ДРУГОЕ ===
  penetration?: number;
  duration?: number;
}
```

---

## 🔗 Связанные документы

- [docs/start_lore.md](./start_lore.md) — Лор мира культивации
- [src/lib/game/combat-system.ts](../src/lib/game/combat-system.ts) — Функции расчёта
- [src/data/presets/technique-presets.ts](../src/data/presets/technique-presets.ts) — Пресеты техник
- [src/types/game.ts](../src/types/game.ts) — Типы игры

---

*Документ обновлён: 2026-02-12 (v2.0)*
