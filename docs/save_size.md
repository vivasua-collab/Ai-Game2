# 📊 Анализ размера хранения данных

**Версия:** 2.0  
**Создано:** 2026-02-27  
**Обновлено:** 2026-02-27  
**Статус:** Расчётный документ

---

## 📋 Постановка задачи

### Ограничения системы:
- **Максимум 100 игровых персонажей** — хранение в полном объёме
- **NPC/Монстры** — соотношение 1:1000 (до 100,000 на 100 игроков)
- **Техники NPC** — только ID, без развития (прокачки)
- **Количество техник** — ограничено активными слотами на уровне

### Слоты техник по уровню:
```
Формула: 3 + (level - 1) боевых слотов + 1 слот культивации

Уровень  Боевые  Культивация  Всего
─────────────────────────────────────
   1       3         1          4
   2       4         1          5
   3       5         1          6
   4       6         1          7
   5       7         1          8
   6       8         1          9
   7       9         1         10
   8      10         1         11
   9      11         1         12
```

---

## 1️⃣ ИГРОВЫЕ ПЕРСОНАЖИ (полное хранение)

### 1.1 Структура Character (полная)

```
Character (текущая схема Prisma):
─────────────────────────────────────────────────────────
id                    String    25 байт
createdAt             DateTime  8 байт
updatedAt             DateTime  8 байт
name                  String    15 байт
strength              Float     8 байт
agility               Float     8 байт
intelligence          Float     8 байт
conductivity          Float     8 байт
cultivationLevel      Int       4 байта
cultivationSubLevel   Int       4 байта
coreCapacity          Int       4 байта
coreQuality           Float     8 байт
currentQi             Int       4 байта
accumulatedQi         Int       4 байта
health                Float     8 байт
fatigue               Float     8 байт
mentalFatigue         Float     8 байт
age                   Int       4 байта
hasAmnesia            Boolean   1 байт
knowsAboutSystem      Boolean   1 байт
currentLocationId     String?   25 байт
sectId                String?   25 байт
sectRole              String?   15 байт
contributionPoints    Int       4 байта
spiritStones          Int       4 байта
cultivationSkills     String    200 байт (JSON)
qiUnderstanding       Int       4 байта
qiUnderstandingCap    Int       4 байта
conductivityMeditations Int     4 байта
fatigueRecoveryMultiplier Float 8 байт
─────────────────────────────────────────────────────────
ИТОГО базовый Character:           ~470 байт
```

### 1.2 Техники игрока (полное хранение)

При уровне культивации 9: максимум **12 техник**

```
Technique (каталог, 1 запись на технику):
─────────────────────────────────────────────────────────
id                    String    25 байт
createdAt             DateTime  8 байт
updatedAt             DateTime  8 байт
name                  String    30 байт
nameId                String    30 байт
description           String    200 байт
type                  String    15 байт
element               String    15 байт
rarity                String    15 байт
level                 Int       4 байта
minLevel              Int       4 байта
maxLevel              Int       4 байта
canEvolve             Boolean   1 байт
minCultivationLevel   Int       4 байта
qiCost                Int       4 байта
physicalFatigueCost   Float     8 байт
mentalFatigueCost     Float     8 байт
statRequirements      String?   100 байт (JSON)
statScaling           String?   100 байт (JSON)
effects               String?   200 байт (JSON)
source                String    15 байт
─────────────────────────────────────────────────────────
ИТОГО Technique:                   ~850 байт

CharacterTechnique (связь игрока с техникой):
─────────────────────────────────────────────────────────
id                    String    25 байт
createdAt             DateTime  8 байт
characterId           String    25 байт
techniqueId           String    25 байт
mastery               Float     8 байт
quickSlot             Int?      4 байта
learningProgress      Float     8 байт
learningSource        String    15 байт
learningStartedAt     DateTime? 8 байт
─────────────────────────────────────────────────────────
ИТОГО CharacterTechnique:          ~130 байт
```

### 1.3 Инвентарь игрока (полное хранение)

```
InventoryItem:
─────────────────────────────────────────────────────────
id                    String    25 байт
createdAt             DateTime  8 байт
characterId           String    25 байт
name                  String    30 байт
nameId                String?   30 байт
description           String?   100 байт
type                  String    20 байт
rarity                String?   15 байт
icon                  String?   10 байт
quantity              Int       4 байта
isConsumable          Boolean   1 байт
useAction             String?   20 байт
durability            Int?      4 байта
maxDurability         Int?      4 байта
qiCharge              Int?      4 байта
maxQiCharge           Int?      4 байта
effects               String?   200 байт (JSON)
properties            String?   200 байт (JSON)
─────────────────────────────────────────────────────────
ИТОГО InventoryItem:              ~700 байт
```

### 1.4 Расчёт на одного игрока (уровень 9)

```
Компонент                    Количество    Размер
──────────────────────────────────────────────────────
Character (базовый)          1             470 байт
GameSession                  1             700 байт
Location (текущая)           1             400 байт
──────────────────────────────────────────────────────
Базовый персонаж                           1,570 байт
──────────────────────────────────────────────────────
Technique (каталог)          12            10,200 байт
  (общий каталог, делится)
CharacterTechnique           12            1,560 байт
──────────────────────────────────────────────────────
Техники                                    11,760 байт
──────────────────────────────────────────────────────
InventoryItem                50            35,000 байт
  (полный инвентарь + экquip)
──────────────────────────────────────────────────────
Инвентарь                                  35,000 байт
──────────────────────────────────────────────────────
ИТОГО на 1 игрока:           ~48 KB
```

### 1.5 Расчёт на 100 игроков

```
Компонент                    Размер
──────────────────────────────────────────────────────
Characters (100 × 1.5 KB)              157 KB
GameSessions (100 × 0.7 KB)            70 KB
Locations (100 × 0.4 KB)               40 KB
──────────────────────────────────────────────────────
База игроков                           267 KB
──────────────────────────────────────────────────────
Техники (каталог, ~200 уникальных)     170 KB
CharacterTechniques (100 × 12 × 130)   156 KB
──────────────────────────────────────────────────────
Техники игроков                        326 KB
──────────────────────────────────────────────────────
InventoryItems (100 × 50 × 700)        3,500 KB
──────────────────────────────────────────────────────
Инвентарь игроков                      3,500 KB
──────────────────────────────────────────────────────
ИТОГО 100 игроков:                     ~4.1 MB
```

---

## 2️⃣ NPC И МОНСТРЫ (хранение по ID)

### 2.1 Структура NPC (упрощённая)

```typescript
// Вместо полного NPC (~2000 байт) храним минималистичную версию

interface NPCMinimal {
  // === ИДЕНТИФИКАЦИЯ ===
  id: string;                  // 25 байт
  name: string;                // 15 байт
  type: 'npc' | 'monster' | 'spirit'; // 10 байт
  
  // === ПОЗИЦИЯ ===
  locationId: string;          // 25 байт
  
  // === УРОВЕНЬ (для генерации) ===
  cultivationLevel: number;    // 4 байта
  
  // === ШАБЛОН (ключевой ID) ===
  templateId: string;          // 25 байт
  variant?: string;            // 10 байт
  
  // === СОСТОЯНИЕ ===
  currentQi: number;           // 4 байта
  health: number;              // 4 байта
  disposition: number;         // 4 байта (отношение к игроку)
  
  // === ТЕХНИКИ (только ID) ===
  techniqueIds: string[];      // 12 × 25 = 300 байт (макс)
  
  // === ЭКИПИРОВКА (только ID) ===
  equipmentIds: string[];      // 10 × 25 = 250 байт (макс)
}

// ИТОГО NPC Minimal: ~700 байт
```

### 2.2 Сравнение структур NPC

```
Подход                         Размер       Экономия
─────────────────────────────────────────────────────
Полный NPC (Character-подобный)  ~2000 байт   0%
Минималистичный (с ID)           ~700 байт    65%
Только ID + seed                 ~150 байт    92%
─────────────────────────────────────────────────────
```

### 2.3 Расчёт на 100,000 NPC

```
Подход                    Количество    Размер         Итого
──────────────────────────────────────────────────────────────
Минималистичный (рекоменд.)
  NPC Minimal             100,000       700 байт      70 MB
  
Только ID + seed
  NPC Ultra-Minimal       100,000       150 байт      15 MB
──────────────────────────────────────────────────────────────
```

### 2.4 Техники NPC (только ID)

NPC не прокачивают техники, поэтому:
- Храним только массив ID техник
- Максимум 12 ID на NPC уровня 9
- Размер: 12 × 25 = 300 байт

```
Техники NPC:
─────────────────────────────────────────────────────────
Вместо:
  Technique × 12             12 × 850 = 10,200 байт
  CharacterTechnique × 12    12 × 130 = 1,560 байт
  
Храним:
  techniqueIds: string[]     12 × 25 = 300 байт
  
Экономия: 97%
─────────────────────────────────────────────────────────
```

### 2.5 Экипировка NPC (только ID)

NPC имеют стандартный набор экипировки:
- Храним только массив ID предметов
- Максимум 10 ID на NPC
- Размер: 10 × 25 = 250 байт

```
Экипировка NPC:
─────────────────────────────────────────────────────────
Вместо:
  InventoryItem × 10         10 × 700 = 7,000 байт
  
Храним:
  equipmentIds: string[]     10 × 25 = 250 байт
  
Экономия: 96%
─────────────────────────────────────────────────────────
```

---

## 3️⃣ КАТАЛОГ ПРЕСЕТОВ (общий для NPC)

### 3.1 Структура пресетов

```
Техники-пресеты (заранее сгенерированные):
─────────────────────────────────────────────────────────
Количество: ~200 базовых техник
Размер: 200 × 850 = 170 KB

Генерируются один раз при старте мира.

Предметы-пресеты:
─────────────────────────────────────────────────────────
Количество: ~300 базовых предметов
Размер: 300 × 700 = 210 KB

Шаблоны NPC:
─────────────────────────────────────────────────────────
Количество: ~50 шаблонов (воин, маг, монстр, торговец...)
Размер: 50 × 2000 = 100 KB
─────────────────────────────────────────────────────────
ИТОГО каталог пресетов:           ~480 KB
```

### 3.2 Модель PresetTechnique

```prisma
model PresetTechnique {
  id          String   @id
  name        String
  nameId      String   @unique
  type        String   // combat, cultivation, support...
  element     String
  rarity      String
  level       Int
  qiCost      Int
  effects     String   // JSON
  
  // Для NPC - фиксированные параметры
  aiPriority  Int      @default(0)  // Приоритет использования ИИ
  aiCondition String?  // Условие активации ИИ
  
  @@map("preset_techniques")
}
```

### 3.3 Модель PresetItem

```prisma
model PresetItem {
  id          String   @id
  name        String
  nameId      String   @unique
  type        String   // weapon, armor, consumable...
  rarity      String
  effects     String   // JSON
  
  @@map("preset_items")
}
```

### 3.4 Модель NpcTemplate

```prisma
model NpcTemplate {
  id              String   @id
  name            String
  type            String   // warrior, mage, beast, spirit...
  baseStats       String   // JSON { strength, agility... }
  techniquePool   String   // JSON ["id1", "id2"...] -候选技术
  equipmentPool   String   // JSON ["id1", "id2"...] -候选装备
  
  @@map("npc_templates")
}
```

---

## 4️⃣ ИТОГОВАЯ ОЦЕНКА БАЗЫ ДАННЫХ

### 4.1 Полная структура

```
┌─────────────────────────────────────────────────────────────┐
│                     БАЗА ДАННЫХ                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  КАТАЛОГ ПРЕСЕТОВ (~480 KB)                          │    │
│  │  ├── PresetTechnique (200 шт)                        │    │
│  │  ├── PresetItem (300 шт)                             │    │
│  │  └── NpcTemplate (50 шт)                             │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  ИГРОВЫЕ ПЕРСОНАЖИ (~4.1 MB)                         │    │
│  │  ├── Character (100 шт) - полное хранение            │    │
│  │  ├── GameSession (100 шт)                            │    │
│  │  ├── Technique (каталог, ~200 шт)                    │    │
│  │  ├── CharacterTechnique (1200 шт)                    │    │
│  │  └── InventoryItem (5000 шт)                         │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  NPC И МОНСТРЫ (15-70 MB)                            │    │
│  │  └── NPC Minimal (100,000 шт)                        │    │
│  │      ├── ID + позиция + состояние                    │    │
│  │      ├── techniqueIds[] (только ID)                  │    │
│  │      └── equipmentIds[] (только ID)                  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  МИР (локации, события, секты)                       │    │
│  │  ├── Location (~1000 шт)                             │    │
│  │  ├── WorldEvent (история)                            │    │
│  │  └── Sect (секты)                                    │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Итоговый размер

```
Компонент                      Размер          Примечание
──────────────────────────────────────────────────────────────
КАТАЛОГ ПРЕСЕТОВ
  PresetTechnique (200)        170 KB
  PresetItem (300)             210 KB
  NpcTemplate (50)             100 KB
──────────────────────────────────────────────────────────────
  Итого каталоги:              480 KB

ИГРОВЫЕ ПЕРСОНАЖИ (100)
  Character + Session + Loc    267 KB
  Техники (каталог + связи)    326 KB
  InventoryItem (5000)         3,500 KB
──────────────────────────────────────────────────────────────
  Итого игроки:                4.1 MB

NPC И МОНСТРЫ (100,000)
  Минималистичный подход       70 MB
  Ultra-minimal (только ID)    15 MB
──────────────────────────────────────────────────────────────
  Итого NPC (рекоменд.):       15-70 MB

МИР
  Locations (1000)             400 KB
  Sects, Events, прочее        500 KB
──────────────────────────────────────────────────────────────
  Итого мир:                   900 KB

──────────────────────────────────────────────────────────────
ИТОГО БАЗА ДАННЫХ:             20-75 MB
  (рекомендуемый диапазон)
──────────────────────────────────────────────────────────────
```

### 4.3 Сравнение с альтернативами

```
Подход                          Размер БД      Память при загрузке
──────────────────────────────────────────────────────────────────
Полное хранение всех объектов   500 MB - 2 GB  1-3 GB
Гибридный (игроки полные)       20-75 MB       100-200 MB
Только ID (все объекты)         5-15 MB        50-100 MB
──────────────────────────────────────────────────────────────────
```

---

## 5️⃣ ПРИНЦИПЫ ГЕНЕРАЦИИ NPC

### 5.1 Процесс создания NPC

```typescript
interface NPCGenerationContext {
  templateId: string;           // Шаблон (воин, маг...)
  level: number;                // Уровень культивации
  locationId: string;           // Где появляется
  variant?: string;             // Вариант шаблона
}

function generateNPC(context: NPCGenerationContext): NPCMinimal {
  // 1. Загружаем шаблон
  const template = getPresetTemplate(context.templateId);
  
  // 2. Определяем количество техник по уровню
  const maxTechniques = 3 + (context.level - 1) + 1; // боевые + культивация
  
  // 3. Выбираем техники из пула шаблона
  const techniqueIds = selectFromPool(
    template.techniquePool,
    maxTechniques,
    context.level
  );
  
  // 4. Выбираем экипировку из пула шаблона
  const equipmentIds = selectFromPool(
    template.equipmentPool,
    10, // макс. экипировка
    context.level
  );
  
  // 5. Генерируем базовые статы
  const stats = generateStats(template.baseStats, context.level);
  
  return {
    id: generateId(),
    name: generateName(template.type),
    type: template.type,
    locationId: context.locationId,
    cultivationLevel: context.level,
    templateId: context.templateId,
    currentQi: stats.maxQi,
    health: 100,
    disposition: 0,
    techniqueIds,
    equipmentIds,
  };
}
```

### 5.2 Восстановление полных данных NPC

```typescript
// При необходимости (бой, детальный просмотр)
function expandNPC(npc: NPCMinimal): NPCFull {
  const template = getPresetTemplate(npc.templateId);
  
  // Загружаем полные данные техник по ID
  const techniques = npc.techniqueIds.map(id => 
    getPresetTechnique(id)
  );
  
  // Загружаем полные данные экипировки по ID
  const equipment = npc.equipmentIds.map(id => 
    getPresetItem(id)
  );
  
  return {
    ...npc,
    techniques,
    equipment,
    stats: calculateStats(template, npc.cultivationLevel),
  };
}
```

---

## 6️⃣ СХЕМА PRISMA (обновлённая)

```prisma
// ==================== ПРЕСЕТЫ ====================

model PresetTechnique {
  id          String   @id
  name        String
  nameId      String   @unique
  description String?
  type        String   // combat, cultivation, support, movement, sensory, healing
  element     String
  rarity      String
  level       Int      @default(1)
  qiCost      Int      @default(0)
  effects     String   // JSON
  
  // ИИ параметры (для NPC)
  aiPriority  Int      @default(0)
  aiCondition String?
  
  createdAt   DateTime @default(now())
  
  @@map("preset_techniques")
}

model PresetItem {
  id          String   @id
  name        String
  nameId      String   @unique
  description String?
  type        String   // weapon, armor, consumable, material, artifact
  subType     String?  // weapon_sword, armor_head...
  rarity      String
  effects     String   // JSON
  icon        String?
  
  createdAt   DateTime @default(now())
  
  @@map("preset_items")
}

model NpcTemplate {
  id              String   @id
  name            String
  type            String   // warrior, mage, beast, spirit, trader...
  baseStats       String   // JSON { strength, agility, intelligence, vitality }
  techniquePool   String   // JSON ["tech_id_1", "tech_id_2"...]
  equipmentPool   String   // JSON ["item_id_1", "item_id_2"...]
  
  // Диапазон уровней
  minLevel        Int      @default(1)
  maxLevel        Int      @default(9)
  
  createdAt       DateTime @default(now())
  
  @@map("npc_templates")
}

// ==================== NPC (МИНИМАЛЬНЫЙ) ====================

model NPC {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // === Базовые данные ===
  name      String
  type      String   // npc, monster, spirit
  
  // === Шаблон ===
  templateId  String
  variant     String?
  
  // === Уровень ===
  cultivationLevel Int @default(1)
  
  // === Позиция ===
  locationId String?
  location   Location? @relation(fields: [locationId], references: [id])
  
  // === Состояние ===
  currentQi     Int   @default(0)
  health        Float @default(100.0)
  disposition   Float @default(0)  // Отношение к игроку (-100 до 100)
  
  // === Техники (только ID) ===
  techniqueIds  String @default("[]")  // JSON array of IDs
  
  // === Экипировка (только ID) ===
  equipmentIds  String @default("[]")  // JSON array of IDs
  
  // === Связи ===
  sessionId String?
  session   GameSession? @relation(fields: [sessionId], references: [id])
  sectId    String?
  sect      Sect?        @relation(fields: [sectId], references: [id])
  
  @@index([type])
  @@index([cultivationLevel])
  @@map("npcs")
}
```

---

## 7️⃣ ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ

### 7.1 Стратегия хранения

```
✅ ХРАНИТЬ ПОЛНОСТЬЮ:
─────────────────────────────────────────────────────────
- Игровые персонажи (макс. 100)
- Каталог техник игрока (созданные/изученные)
- Инвентарь игроков
- Состояния сессий
─────────────────────────────────────────────────────────

✅ ХРАНИТЬ ПО ID:
─────────────────────────────────────────────────────────
- NPC и монстры (до 100,000)
- Техники NPC (массив ID)
- Экипировка NPC (массив ID)
─────────────────────────────────────────────────────────

✅ ГЕНЕРИРОВАТЬ ЗАРАНЕЕ:
─────────────────────────────────────────────────────────
- Пресеты техник (~200 шт)
- Пресеты предметов (~300 шт)
- Шаблоны NPC (~50 шт)
─────────────────────────────────────────────────────────
```

### 7.2 Оперативная память

```
При загрузке мира:
─────────────────────────────────────────────────────────
Компонент                     Размер
─────────────────────────────────────────────────────────
Каталог пресетов              ~500 KB
Игроки (все 100)              ~5 MB
NPC в активной зоне (~1000)   ~700 KB
Текущая локация               ~50 KB
─────────────────────────────────────────────────────────
ИТОГО:                        ~6-7 MB
─────────────────────────────────────────────────────────

Потенциальная нагрузка:
- 100 игроков одновременно: ~10 MB
- Бой с 10 NPC: +50 KB (расширение данных)
─────────────────────────────────────────────────────────
```

### 7.3 Производительность

```
Операция                      Время (оценка)
─────────────────────────────────────────────────────────
Загрузка 1 игрока             10-20 мс
Загрузка 1 NPC (minimal)      1-2 мс
Расширение NPC до полного     5-10 мс
Генерация NPC                 2-5 мс
Поиск NPC в зоне              5-10 мс (с индексом)
─────────────────────────────────────────────────────────
```

---

## 📊 ИТОГОВАЯ СВОДКА

### Размер базы данных:

| Компонент | Количество | Размер |
|-----------|------------|--------|
| **Пресеты** | | |
| PresetTechnique | 200 | 170 KB |
| PresetItem | 300 | 210 KB |
| NpcTemplate | 50 | 100 KB |
| **Игроки** | | |
| Character + связи | 100 | 4.1 MB |
| **NPC** | | |
| NPC Minimal | 100,000 | 15-70 MB |
| **Мир** | | |
| Locations + прочее | ~1000 | 900 KB |
| **ИТОГО** | | **20-75 MB** |

### Ключевые решения:

1. **100 игроков** — полное хранение, техника ограничена слотами (макс 12 на ур.9)
2. **100,000 NPC** — хранение по ID, без прокачки техник
3. **Соотношение 1:1000** — оптимальное для игрового мира
4. **Каталог пресетов** — генерируется заранее, общий для всех
5. **Экономия** — 85-95% по сравнению с полным хранением

---

*Документ версия 2.0 — обновлён с учётом ограничений:*
- *Макс. 100 игроков*
- *Соотношение игрок:NPC = 1:1000*
- *Техники NPC без прокачки*
- *Ограничение техник по слотам уровня*
