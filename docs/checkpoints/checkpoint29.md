# 📋 Checkpoint 29 — Системы Предметов

**Версия:** 2.0
**Создано:** 2026-03-01
**Обновлено:** 2026-03-01
**Статус:** План разработки

---

## 📋 Обзор

Документ описывает план реализации разделённых систем предметов с генераторами для каждого типа. Основан на архитектуре генератора техник.

### Ключевые принципы
- **Генераторы работают в React среде**, не используя Event Bus
- **Базовый референс**: `technique-generator.ts`
- **Система ID**: префиксы уже определены в `id-system.md`
- **Лор первичен**: `docs/start_lore.md` — главный источник правил

---

## 1️⃣ СИСТЕМЫ ПРЕДМЕТОВ

### 1.1 Оружие (Weapon System)
**Префикс ID:** `WP`

**Категории:**
- Одноручное клинковое (one_handed_blade)
- Двуручное клинковое (two_handed_blade)
- Древковое (polearm)
- Дробящее (blunt)
- Кистевое (fist)
- Метательное (thrown)
- Дальнобойное (ranged)

**Параметры:**
```typescript
interface Weapon {
  id: string;           // WP_XXXXXX
  name: string;
  nameEn: string;

  // Классификация
  category: WeaponCategory;
  weaponType: WeaponType;

  // Базовые параметры
  baseDamage: number;
  baseRange: number;    // в метрах
  attackSpeed: number;  // атаки в секунду

  // Требования
  requirements: {
    strength?: number;
    agility?: number;
    cultivationLevel?: number;
  };

  // Свойства
  properties: {
    critChance: number;
    critDamage: number;
    armorPenetration: number;
  };

  // Редкость
  rarity: Rarity;

  // Улучшения (битовое поле)
  upgradeFlags: number;  // 0-15 (4 бита)

  // Сет (заглушка)
  setId?: string;
  isSetItem: boolean;
}
```

---

### 1.2 Экипировка (Equipment System)
**Префикс ID:** `AR`

#### Слоты экипировки:

| Часть тела | Слот | Описание |
|------------|------|----------|
| Голова | `head` | Шлемы, капюшоны, обручи |
| Торс | `torso` | Кирасы, доспехи, роба |
| Ноги | `legs` | Штаны, поножи |
| Стопы | `feet` | Ботинки, сапоги |
| Кисти | `hands_gloves` | Перчатки, рукавицы |
| Руки | `hands_bracers` | Защитные рукава, наручи |

**Важно:** Кисти имеют ДВА слота:
- `hands_gloves` — перчатки
- `hands_weapon` — оружие (или инструмент)

**Параметры:**
```typescript
interface Armor {
  id: string;           // AR_XXXXXX
  name: string;

  // Слот
  slot: EquipmentSlot;

  // Защита
  defense: {
    physical: number;
    qi: number;
    elemental: Record<Element, number>;
  };

  // Характеристики
  stats: {
    strength?: number;
    agility?: number;
    conductivity?: number;
  };

  // Требования
  requirements: {
    cultivationLevel?: number;
  };

  // Редкость
  rarity: Rarity;

  // Улучшения (битовое поле)
  upgradeFlags: number;  // 0-15 (4 бита)

  // Сет (заглушка)
  setId?: string;
  isSetItem: boolean;
}

type EquipmentSlot =
  | 'head'
  | 'torso'
  | 'legs'
  | 'feet'
  | 'hands_gloves'
  | 'hands_bracers';
```

---

### 1.3 Аксессуары (Accessories System)
**Префикс ID:** `AC`

#### Слоты аксессуаров:

| Тип | Слот | Количество | Описание |
|-----|------|------------|----------|
| Кольца | `ring_left_1`, `ring_left_2` | 2 | Левая рука |
| Кольца | `ring_right_1`, `ring_right_2` | 2 | Правая рука |
| Амулет | `amulet` | 1 | Шея |
| Талисманы | `talisman_1`, `talisman_2` | **2** | ✅ Уточнено |

**Параметры:**
```typescript
interface Accessory {
  id: string;           // AC_XXXXXX
  name: string;

  // Слот
  slot: AccessorySlot;

  // Бонусы (не для талисманов!)
  bonuses: {
    stats: {
      strength?: number;
      agility?: number;
      intelligence?: number;
      conductivity?: number;
    };
    special?: SpecialBonus[];
  };

  // Редкость
  rarity: Rarity;

  // Улучшения (битовое поле)
  upgradeFlags: number;  // 0-15 (4 бита)

  // Сет (заглушка)
  setId?: string;
  isSetItem: boolean;
}

type AccessorySlot =
  | 'ring_left_1'
  | 'ring_left_2'
  | 'ring_right_1'
  | 'ring_right_2'
  | 'amulet'
  | 'talisman_1'
  | 'talisman_2';
```

#### Талисманы (УТОЧНЕНО):

```typescript
interface Talisman extends Accessory {
  // ❌ НЕ добавляют Ци
  // ❌ НЕ дают бонусы к статам/характеристикам
  // ✅ Обрезанная копия формаций
  // ✅ Одноразовые
  // ✅ Минимальное время действия
  // ✅ Ситуативное использование

  // Тип эффекта талисмана
  effect: {
    type: TalismanEffectType;
    duration: number;       // секунды (минимальное)
    radius?: number;        // радиус действия (если AoE)
  };

  // Одноразовость
  isConsumable: true;
  maxUses: 1;
  currentUses: number;
}

type TalismanEffectType =
  | 'detection'       // Обнаружение (врагов, ловушек, Ци)
  | 'protection'      // Кратковременный щит
  | 'enhancement'     // Усиление восприятия
  | 'concealment'     // Сокрытие присутствия
  | 'barrier'         // Временный барьер
  | 'purification';   // Очистка от лёгких эффектов
```

---

### 1.4 Расходные материалы (Consumables System)
**Префикс ID:** `CS`

**Типы:**
- Таблетки (pills) — кратковременные баффы
- Эликсиры (elixirs) — долгосрочные эффекты
- Еда (food) — восстановление
- Свитки (scrolls) — одноразовые эффекты

**Важно:** Расходники **НЕ добавляют Ци** — это задача зарядников.

#### Пояс (Belt System) — УТОЧНЕНО:

**Концепция:** Пояс имеет слоты двух типов:
- Слоты камней Ци (для зарядника)
- Слоты быстрого доступа (для расходников)

**Активация слотов быстрого доступа:**
- Слоты быстрого доступа заняты техниками (цифры 1-9)
- **Решение:** Активация через `CTRL + цифра` (требуется тестирование!)

```typescript
interface Belt {
  id: string;           // AR_XXXXXX (это экипировка)
  name: string;

  // Слоты
  slots: {
    qiStones: number;      // Количество слотов под камни Ци
    quickAccess: number;   // До 4 слотов быстрого доступа
  };

  // Параметры
  capacity: number;        // Общая вместимость
}

interface QuickAccessSlot {
  slotIndex: number;       // 1-4
  item: Consumable | null;
  hotkey: string;          // "CTRL+1", "CTRL+2", "CTRL+3", "CTRL+4"
}

interface Consumable {
  id: string;           // CS_XXXXXX
  name: string;

  // Тип
  type: ConsumableType;

  // Эффект
  effect: {
    type: ConsumableEffectType;
    value: number;
    duration?: number;     // в секундах
  };

  // Использование
  usage: {
    castTime: number;      // время использования
    cooldown?: number;
  };

  // Редкость
  rarity: Rarity;
}

type ConsumableType = 'pill' | 'elixir' | 'food' | 'scroll';
type ConsumableEffectType =
  | 'heal_hp'           // Восстановление здоровья
  | 'heal_stamina'      // Восстановление сил
  | 'buff_stat'         // Усиление характеристики
  | 'buff_resistance'   // Усиление сопротивления
  | 'cure'              // Лечение статуса
  | 'special';          // Особый эффект
```

---

### 1.5 Камни Ци (Qi Stones System) — УТОЧНЕНО
**Префикс ID:** `QS`

#### ✅ РЕШЕНО: Качество удалено

**Причина:** Лор (`start_lore.md`) первичен. В Лоре нет понятия качества камней.

**Камень Ци характеризуется только:**
1. **Объём Ци** (ед)
2. **Тип Ци** (calm / chaotic)

**Документация обновлена:** `docs/qi_stone.md` v2.0

```typescript
interface QiStone {
  id: string;           // QS_XXXXXX
  name: string;

  // Размер
  sizeClass: QiStoneSize;
  volumeCm3: number;
  surfaceCm2: number;

  // Содержание Ци
  totalQi: number;      // Полное содержание
  currentQi: number;    // Текущее (после использования)

  // Тип (ЕДИНСТВЕННЫЙ параметр классификации)
  type: 'calm' | 'chaotic';

  // ❌ Качество УДАЛЕНО (Лор первичен)

  // Состояние
  isSealed: boolean;
}

type QiStoneSize =
  | 'dust'      // < 0.1 см³, < 102 ед Ци
  | 'fragment'  // 0.1 - 1 см³, 102 - 1024 ед
  | 'small'     // 1 - 8 см³, 1024 - 8192 ед
  | 'medium'    // 8 - 27 см³, 8192 - 27648 ед
  | 'large'     // 27 - 64 см³, 27648 - 65536 ед
  | 'huge'      // 64 - 125 см³, 65536 - 128000 ед
  | 'boulder';  // > 125 см³, > 128000 ед
```

**Проблема высоких уровней — решения:**
1. Камни большего объёма (sizeClass: dust → boulder)
2. Крупные камни для зарядников (boulder = до 128000+ ед Ци)

---

### 1.6 Зарядники (Chargers System)
**Префикс ID:** `CH`

#### Концепция:

1. **Отдельный слот** — не привязан к части тела
2. **Заряжается в инвентаре** — не в бою
3. **Вставляется в слот** — до исчерпания камней

#### Ограничение эффективности (⚠️ ВАЖНО):

Из `docs/start_lore.md` — **Сохранение энергии строго соблюдается**:

> Эффективность зарядника **НЕ может быть больше 100%** — иначе нарушается правило сохранения Ци.

```typescript
interface Charger {
  id: string;           // CH_XXXXXX
  name: string;

  // Параметры
  capacity: number;        // Сколько камней вмещает
  efficiency: number;      // 0.5 - 1.0 (50% - 100%) — НИКОГДА > 100%!
  chargeRate: number;      // Скорость отдачи Ци (ед/сек)

  // Состояние
  installed: boolean;
  installedStones: QiStone[];
  totalQiRemaining: number;

  // Требования
  requirements: {
    cultivationLevel?: number;
  };

  // Редкость
  rarity: Rarity;

  // Улучшения (битовое поле)
  upgradeFlags: number;  // 0-15 (4 бита)
}
```

**Пример работы:**
```
Зарядник (efficiency = 80%):
- Камень: 1000 ед Ци
- Практик получит: 800 ед Ци
- Потери: 200 ед Ци (рассеиваются)
```

---

## 2️⃣ ГЕНЕРАТОРЫ ПРЕДМЕТОВ

### 2.1 Архитектура генератора

Базируется на `technique-generator.ts`:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     АРХИТЕКТУРА ГЕНЕРАТОРОВ                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   base-item-generator.ts (БАЗА)                                         │
│   ├── Общие утилиты (seededRandom, weightedSelect)                      │
│   ├── Система ID (idService)                                            │
│   ├── Система редкости (RARITY_MULTIPLIERS)                             │
│   └── Система имён (NAME_PARTS)                                         │
│                                                                          │
│   ├── weapon-generator.ts                                               │
│   ├── armor-generator.ts                                                │
│   ├── accessory-generator.ts                                            │
│   ├── consumable-generator.ts                                           │
│   ├── qi-stone-generator.ts                                             │
│   └── charger-generator.ts                                              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Унифицированный BaseItemGenerator (ОТЛОЖЕНО)

**Статус:** ⚠️ Требуется тестовый режим после готовности отдельных генераторов

**Концепция:**
- Один общий генератор
- Несколько пресетов, разнесённых по вкладкам
- В зависимости от вкладки активируются нужные маршруты

**Причина отсрочки:**
> "у меня сомнения, что с текущим контекстом модели получится реализовать такой сложный модуль"

---

## 3️⃣ СИСТЕМА УЛУЧШЕНИЙ — УТОЧНЕНО

### Статус: Заглушки добавлены

**Реализация:**
- **Битовое поле** (4 бита = значения 0-15)
- Минимальное использование памяти
- Расширяемость в будущем

```typescript
// Поле добавлено во все предметы:
interface UpgradableItem {
  // ...
  upgradeFlags: number;  // 0-15 (4 бита)
}

// Битовые флаги:
const UPGRADE_FLAGS = {
  NONE: 0,           // 0000 - без улучшений
  REINFORCED: 1,     // 0001 - усиление
  ENCHANTED: 2,      // 0010 - зачарование
  REFINED: 4,        // 0100 - очистка
  MASTERWORK: 8,     // 1000 - мастерская работа
  // Комбинации:
  REINFORCED_ENCHANTED: 3,   // 0011
  REFINED_MASTERWORK: 12,    // 1100
  MAX: 15,           // 1111 - все улучшения
};

// Проверка флага:
function hasUpgrade(flags: number, upgrade: number): boolean {
  return (flags & upgrade) === upgrade;
}

// Установка флага:
function addUpgrade(flags: number, upgrade: number): number {
  return flags | upgrade;
}
```

---

## 4️⃣ СИСТЕМА СЕТОВ (ДАЛЬНИЕ ПЛАНЫ)

### Статус: Отложено

**Поля-заглушки (добавить сейчас):**
```typescript
interface ItemWithSet {
  // ...
  setId?: string;        // ИД сета
  isSetItem: boolean;    // Является ли вещь сетовой
}
```

**Сложность внедрения:**
- Затрагивает разные типы экипировки
- Требует сборщик сетов на основе существующих вещей
- Нужен общий генератор с выбором "создать сет + список вещей"

---

## 5️⃣ ГЕНЕРАТОР ИМЁН (БЛИЖАЙШАЯ РЕАЛИЗАЦИЯ)

### Проблема:
> "Холодный Вода" — плохо звучит, по сравнению с "Холодная Вода"

### Решение:
Создать систему генерации имён с учётом:
1. **Род слова** (мужской/женский/средний)
2. **Согласование прилагательных**
3. **База прилагательных + существительных по типам**

```typescript
interface NameGenerator {
  // База прилагательных с родом
  adjectives: {
    male: string[];    // "Холодный", "Огненный", "Тяжёлый"
    female: string[];  // "Холодная", "Огненная", "Тяжёлая"
    neuter: string[];  // "Холодное", "Огненное", "Тяжёлое"
  };

  // База существительных с родом
  nouns: {
    male: Array<{ word: string; type: ItemType }>;    // "Клинок", "Меч"
    female: Array<{ word: string; type: ItemType }>;  // "Вода", "Рука"
    neuter: Array<{ word: string; type: ItemType }>;  // "Кольцо", "Зеркало"
  };

  // Генерация
  generate(itemType: ItemType, element?: Element): string;
}

// Пример:
// Адъективы: "Холодный" (m), "Холодная" (f), "Холодное" (n)
// Существительные: "Клинок" (m), "Вода" (f), "Кольцо" (n)
// Результат: "Холодный Клинок", "Холодная Вода" ✅
```

---

## 6️⃣ ПЛАН РЕАЛИЗАЦИИ

### Фаза 1: Базовая инфраструктура
- [ ] Создать `base-item-generator.ts` с общими утилитами
- [ ] Создать `item-config.ts` с конфигурацией типов
- [ ] Обновить `id-service.ts` для предметов
- [ ] Создать генератор имён с учётом рода

### Фаза 2: Генератор оружия
- [ ] Создать `weapon-generator.ts`
- [ ] Создать `WeaponGeneratorPanel.tsx`
- [ ] Интегрировать в меню "Создание"

### Фаза 3: Генератор экипировки
- [ ] Создать `armor-generator.ts`
- [ ] Создать `ArmorGeneratorPanel.tsx`
- [ ] Реализовать систему слотов

### Фаза 4: Генератор аксессуаров
- [ ] Создать `accessory-generator.ts`
- [ ] Создать `AccessoryGeneratorPanel.tsx`
- [ ] Реализовать механику талисманов

### Фаза 5: Генератор расходников
- [ ] Создать `consumable-generator.ts`
- [ ] Создать `ConsumableGeneratorPanel.tsx`
- [ ] Реализовать систему пояса с CTRL-активацией

### Фаза 6: Генератор камней Ци
- [x] ~~Разрешить противоречие по качеству~~ — **РЕШЕНО: качество удалено**
- [ ] Создать `qi-stone-generator.ts`
- [ ] Создать `QiStoneGeneratorPanel.tsx`

### Фаза 7: Генератор зарядников
- [ ] Создать `charger-generator.ts`
- [ ] Создать `ChargerGeneratorPanel.tsx`
- [ ] Реализовать механику заряда/разряда

### Фаза 8: Интеграция
- [ ] Объединить генераторы в UI
- [ ] Протестировать CTRL-активацию пояса
- [ ] Документация

---

## 🔗 Связанные документы

- [id-system.md](./id-system.md) — Система ID
- [qi_stone.md](./qi_stone.md) — Камни Ци (v2.0 — без качества)
- [start_lore.md](./start_lore.md) — Лор мира (первичен!)

---

## 📝 История изменений

| Версия | Дата | Изменения |
|--------|------|-----------|
| 2.0 | 2026-03-01 | Уточнения: талисманы (2 слота), пояс (CTRL+цифра), улучшения (биты 0-15), качество камней удалено |
| 1.0 | 2026-03-01 | Первоначальная версия |

---

*Документ создан: 2026-03-01*
*Обновлён: 2026-03-01*
