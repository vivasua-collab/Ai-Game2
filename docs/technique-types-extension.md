# 🎭 Расширенная классификация техник

**Версия:** 2.0  
**Создано:** 2026-02-27  
**Статус:** Проект

---

## 📋 Обзор

Документ расширяет систему классификации техник с учётом:
1. Выделения защитных техник в отдельный тип
2. Добавления типа "Проклятия" (длительные и боевые)
3. Проработки системы отравлений

---

## 1️⃣ ТЕКУЩАЯ ПРОБЛЕМА: ЩИТОВЫЕ ТЕХНИКИ

### 1.1 Текущее состояние

```
type: "combat"
combatType: "defense_shield" | "defense_block" | "defense_dodge"
```

**Проблема:** Защитные техники классифицируются как боевые (TC), хотя по сути являются защитными.

### 1.2 Предлагаемое решение

Вынести защитные техники в отдельный тип **defense**:

```typescript
type TechniqueType = 
  | "combat"       // Атакующие техники
  | "defense"      // Защитные техники (бывшие defense_*)
  | "cultivation"  // Культивация
  | "support"      // Поддержка
  | "movement"     // Перемещение
  | "sensory"      // Восприятие
  | "healing"      // Исцеление
  | "curse"        // Проклятия (НОВОЕ)
  | "poison";      // Отравления (НОВОЕ)
```

---

## 2️⃣ НОВАЯ СИСТЕМА ТИПОВ

### 2.1 Обновлённые префиксы ID

| Тип техники | Префикс | Описание |
|-------------|---------|----------|
| combat | TC | Атакующие боевые техники |
| defense | DF | Защитные техники |
| cultivation | CU | Техники культивации |
| support | SP | Поддержка (баффы) |
| movement | MV | Перемещение |
| sensory | SN | Восприятие |
| healing | HL | Исцеление |
| **curse** | **CR** | **Проклятия** |
| **poison** | **PN** | **Отравления** |

### 2.2 Подтипы защитных техник (defense)

```typescript
type DefenseSubtype = 
  | "shield"     // Энергетический щит
  | "barrier"    // Стационарный барьер
  | "block"      // Активный блок
  | "dodge"      // Уклонение
  | "absorb"     // Поглощение урона
  | "reflect";   // Отражение урона
```

---

## 3️⃣ СИСТЕМА ПРОКЛЯТИЙ

### 3.1 Классификация по длительности

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     ТИПЫ ПРОКЛЯТИЙ                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────────┐      ┌─────────────────────┐                  │
│   │   БОЕВЫЕ ПРОКЛЯТИЯ   │      │  РИТУАЛЬНЫЕ ПРОКЛЯТИЯ│                  │
│   │   (Combat Curses)    │      │  (Ritual Curses)     │                  │
│   ├─────────────────────┤      ├─────────────────────┤                  │
│   │ • Длительность:     │      │ • Длительность:     │                  │
│   │   секунды-минуты    │      │   часы-месяцы       │                  │
│   │ • Применение:       │      │ • Применение:       │                  │
│   │   мгновенно в бою   │      │   ритуал/формация   │                  │
│   │ • Снимается:        │      │ • Снимается:        │                  │
│   │   стандартными      │      │   сложными методами │                  │
│   │   методами          │      │                     │                  │
│   │ • Ци-стоимость:     │      │ • Ци-стоимость:     │                  │
│   │   низкая-средняя    │      │   высокая           │                  │
│   └─────────────────────┘      └─────────────────────┘                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Боевые проклятия (Combat Curses)

**Характеристики:**
- Длительность: 10 сек — 5 минут
- Применение: мгновенно в бою
- Слот: боевой слот техники
- Контратакуемо: да

```typescript
interface CombatCurse {
  subtype: 'combat';
  
  // Длительность в секундах
  duration: number;
  
  // Типы эффектов
  effect: CurseEffect;
  
  // Можно ли отразить
  reflectable: boolean;
  
  // Шанс сопротивления
  resistanceStat: 'willpower' | 'cultivationLevel' | 'conductivity';
}

type CurseEffectType =
  | 'weakness'      // Снижение силы
  | 'slowness'      // Замедление
  | 'blindness'     // Слепота
  | 'silence'       // Блокировка техник
  | 'confusion'     // Путаница
  | 'fear'          // Страх
  | 'exhaustion'    // Истощение
  | 'qi_drain'      // Истощение Ци
  | 'soul_burn';    // Жжение души (DoT)
```

**Примеры боевых проклятий:**

| Название | Уровень | Эффект | Длительность | Ци |
|----------|---------|--------|--------------|-----|
| "Теневое удушье" | 2 | Снижение Ци на 30% | 30 сек | 25 |
| "Печать молчания" | 3 | Блокировка техник | 20 сек | 40 |
| "Страх смерти" | 4 | -50% к атаке, страх | 1 мин | 60 |
| "Касание пустоты" | 6 | DoT 5/сек, -20% защиты | 2 мин | 100 |

### 3.3 Ритуальные проклятия (Ritual Curses)

**Характеристики:**
- Длительность: часы, дни, месяцы
- Применение: через ритуал/формацию
- Требует: специальные условия, время, ингредиенты
- Снимается: только ритуалом очищения

```typescript
interface RitualCurse {
  subtype: 'ritual';
  
  // Длительность
  duration: {
    min: number;  // Минимум в часах
    max: number;  // Максимум в часах (0 = бесконечно)
  };
  
  // Условия наложения
  requirements: {
    ritualTime: number;        // Время ритуала (минуты)
    qiCost: number;            // Стоимость Ци
    ingredients?: string[];    // Необходимые ингредиенты
    targetCondition?: {        // Условие на цели
      type: 'bound' | 'willing' | 'named' | 'blood_sample';
    };
    casterCondition?: {
      minLevel?: number;
      element?: Element;
    };
  };
  
  // Снятие
  removal: {
    method: 'ritual' | 'artifact' | 'higher_cultivator' | 'time' | 'death';
    difficulty: number;  // 1-10
  };
  
  // Эффект
  effect: RitualCurseEffect;
}

type RitualCurseEffect =
  | 'cultivation_block'    // Блокировка культивации
  | 'meridian_damage'      // Повреждение меридиан
  | 'core_corruption'      // Разрушение ядра
  | 'bloodline_suppress'   // Подавление родословной
  | 'fate_binding'         // Связывание судьбы
  | 'soul_seal'            // Печать души
  | 'karma_curse'          // Кармическое проклятие
  | 'elemental_imbalance'; // Дисбаланс элементов
```

**Примеры ритуальных проклятий:**

| Название | Уровень | Эффект | Длительность | Ритуал |
|----------|---------|--------|--------------|--------|
| "Печать падения" | 5 | -50% к культивации | 30 дней | 4 часа |
| "Кровавая метка" | 4 | Отслеживание жертвы | Бесконечно | 1 час |
| "Проклятие бессилия" | 6 | Блокировка техник 5+ | 7 дней | 6 часов |
| "Душевные оковы" | 8 | Контроль воли | Бесконечно | 24 часа |

---

## 4️⃣ СИСТЕМА ОТРАВЛЕНИЙ

### 4.1 Классификация по цели

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     ТИПЫ ОТРАВЛЕНИЙ                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────────┐      ┌─────────────────────┐                  │
│   │  ОТРАВЛЕНИЕ ТЕЛА    │      │   ОТРАВЛЕНИЕ ЦИ     │                  │
│   │  (Body Poison)      │      │   (Qi Poison)       │                  │
│   ├─────────────────────┤      ├─────────────────────┤                  │
│   │ • Воздействует на:  │      │ • Воздействует на:  │                  │
│   │   физическое тело   │      │   меридианы и ядро  │                  │
│   │ • Симптомы:         │      │ • Симптомы:         │                  │
│   │   HP, органы,       │      │   Ци, проводимость, │                  │
│   │   выносливость      │      │   техники           │                  │
│   │ • Лечение:          │      │ • Лечение:          │                  │
│   │   медицина, Ци      │      │   очищение Ци       │                  │
│   │ • Уровни культивации│      │ • Только культиваторы│                  │
│   │   повышают          │      │                     │                  │
│   │   сопротивление     │      │                     │                  │
│   └─────────────────────┘      └─────────────────────┘                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 Отравление тела (Body Poison)

```typescript
interface BodyPoison {
  // Тип отравления
  delivery: 'ingestion' | 'contact' | 'injection' | 'inhalation';
  
  // Стадии отравления
  stages: PoisonStage[];
  
  // Сопротивление
  resistance: {
    stat: 'constitution' | 'cultivationLevel';
    reductionPerLevel: number;  // % снижения эффекта за уровень
  };
  
  // Лечение
  treatment: {
    medical: number;    // Шанс медициной (%)
    qiPurification: number;  // Шанс очищением Ци (%)
    naturalRecovery: number;  // Время естественного восстановления (часы)
  };
}

interface PoisonStage {
  onset: number;        // Время наступления (минуты)
  duration: number;     // Длительность стадии (минуты)
  
  effects: {
    hpDamage?: number;        // Урон HP/ход
    fatigue?: number;         // Усталость (%)
    statReduction?: {         // Снижение характеристик
      strength?: number;
      agility?: number;
      intelligence?: number;
    };
    organDamage?: string;     // Повреждение органа
    paralysis?: number;       // Паралич (%)
    death?: boolean;          // Смертельный исход
  };
}
```

**Примеры ядов тела:**

| Название | Уровень | Доставка | Стадии | Лечение |
|----------|---------|----------|--------|---------|
| "Зелёный туман" | 1 | Вдыхание | 1 стадия | Легко |
| "Кровавый корень" | 3 | Употребление | 3 стадии | Средне |
| "Слёзы вдовы" | 5 | Контакт | 2 стадии | Сложно |
| "Пустотный яд" | 7 | Инъекция | 4 стадии | Экстремально |

### 4.3 Отравление Ци (Qi Poison)

```typescript
interface QiPoison {
  // Специфика для культиваторов
  cultivationSpecific: true;
  
  // Способы заражения
  delivery: 
    | 'technique'       // Через технику противника
    | 'contaminated_qi' // Заражённая Ци в среде
    | 'cursed_item'     // Проклятый предмет
    | 'blood_transfusion'; // Переливание крови
  
  // Эффекты на Ци-систему
  effects: {
    qiCorruption?: number;     // % Corruption Ци за ход
    meridianDamage?: number;   // Урон меридианам
    conductivityReduction?: number;  // % снижение проводимости
    coreInstability?: number;  // % нестабильности ядра
    techniqueBlock?: string[]; // Заблокированные типы техник
    elementCorruption?: {      // Искажение элемента
      from: Element;
      to: Element;
    };
  };
  
  // Очищение
  purification: {
    meditationModifier: number;  // Множитель эффективности медитации
    specialTechniques: string[]; // Необходимые техники
    artifactHelper?: string;     // Помощник-артефакт
    higherCultivator?: {
      minLevel: number;
      timeRequired: number;
    };
  };
}
```

**Примеры ядов Ци:**

| Название | Уровень | Эффект | Заражение | Очищение |
|----------|---------|--------|-----------|----------|
| "Туман разложения" | 4 | -20% проводимости | Техника | Медитация 3x |
| "Коррозия меридиан" | 5 | Урон меридианам | Заражённая Ци | Техника + 2 часа |
| "Печать элемента" | 6 | Блокировка элемента | Проклятый предмет | Культиivator 8+ |
| "Гниение ядра" | 8 | Разрушение ядра | Техника | Ритуал + артефакт |

---

## 5️⃣ МЕХАНИКА СОПРОТИВЛЕНИЯ

### 5.1 Формула сопротивления проклятиям

```typescript
function calculateCurseResistance(
  target: Character,
  curse: CombatCurse | RitualCurse
): number {
  // Базовое сопротивление от уровня культивации
  let resistance = target.cultivationLevel * 5;  // 5% за уровень
  
  // Бонус от воли (если есть характеристика)
  if (target.willpower) {
    resistance += (target.willpower - 10) * 2;
  }
  
  // Бонус от проводимости (понимание Ци помогает сопротивляться)
  resistance += target.conductivity * 10;
  
  // Модификатор от элемента (пустота лучше сопротивляется проклятиям)
  if (target.primaryElement === 'void') {
    resistance += 15;
  }
  
  // Штраф от уже активных проклятий
  const activeCurses = target.buffs?.filter(b => b.type === 'curse') || [];
  resistance -= activeCurses.length * 10;
  
  return Math.max(0, Math.min(95, resistance));  // 0-95%
}
```

### 5.2 Формула сопротивления ядам

```typescript
function calculatePoisonResistance(
  target: Character,
  poison: BodyPoison | QiPoison
): number {
  // Базовое сопротивление
  let resistance = 0;
  
  // Конституция (для ядов тела)
  if ('stages' in poison) {  // BodyPoison
    resistance += (target.constitution || 10) * 2;
    resistance += target.cultivationLevel * 8;  // Культивация даёт иммунитет
  }
  
  // Проводимость и контроль Ци (для ядов Ци)
  if ('qiCorruption' in poison) {  // QiPoison
    resistance += target.conductivity * 20;
    resistance += target.cultivationLevel * 10;
    
    // Бонус от техник очищения
    const purificationTechs = target.techniques?.filter(
      t => t.technique.effects?.qiPurification
    ) || [];
    resistance += purificationTechs.length * 15;
  }
  
  return Math.max(0, Math.min(90, resistance));  // 0-90%
}
```

---

## 6️⃣ СЛОТЫ ТЕХНИК

### 6.1 Обновлённая система слотов

```typescript
interface TechniqueSlots {
  // Слот культивации (1 техника)
  cultivationSlot: string | null;
  
  // Боевые слоты (растут с уровнем: 3 + (level-1))
  combatSlots: (string | null)[];
  
  // Новое: Слот проклятий (отдельный!)
  curseSlot: string | null;  // 1 слот для проклятия
  
  // Новое: Слот формаций
  formationSlot: string | null;  // 1 активная формация
}

// Какие типы техник в какие слоты
const SLOT_ASSIGNMENT = {
  cultivation: ['cultivation'],
  combat: ['combat', 'defense', 'support', 'movement', 'sensory', 'healing', 'poison'],
  curse: ['curse'],  // Проклятия только в специальный слот
  formation: ['formation'],
};
```

### 6.2 Почему проклятия в отдельный слот?

1. **Баланс**: Игрок не может загрузить все слоты проклятиями
2. **Тактика**: Выбор между проклятием и защитой
3. **Лор**: Проклятия требуют особого канала Ци
4. **Риск**: Слот занят даже если проклятие не активно

---

## 7️⃣ ИНТЕГРАЦИЯ С ГЕНЕРАТОРОМ

### 7.1 Обновлённые типы в генераторе

```typescript
// technique-generator.ts

export type TechniqueType = 
  | "combat" 
  | "defense"      // НОВОЕ: вынесено из combat
  | "cultivation" 
  | "support" 
  | "movement" 
  | "sensory" 
  | "healing"
  | "curse"        // НОВОЕ
  | "poison";      // НОВОЕ

export type CurseType = 'combat' | 'ritual';
export type PoisonType = 'body' | 'qi';

// Обновлённые префиксы ID
const TYPE_ID_PREFIX: Record<TechniqueType, string> = {
  combat: 'TC',
  defense: 'DF',   // НОВОЕ
  cultivation: 'CU',
  support: 'SP',
  movement: 'MV',
  sensory: 'SN',
  healing: 'HL',
  curse: 'CR',     // НОВОЕ
  poison: 'PN',    // НОВОЕ
};
```

### 7.2 Генерация проклятий

```typescript
function generateCurse(
  id: string,
  level: number,
  curseType: CurseType,
  seed?: number
): GeneratedCurse {
  const actualSeed = seed ?? Date.now();
  const rng = seededRandom(actualSeed);
  
  if (curseType === 'combat') {
    return generateCombatCurse(id, level, rng);
  } else {
    return generateRitualCurse(id, level, rng);
  }
}

function generateCombatCurse(
  id: string,
  level: number,
  rng: () => number
): CombatCurse {
  const effects: CurseEffectType[] = [
    'weakness', 'slowness', 'blindness', 'silence',
    'confusion', 'fear', 'exhaustion', 'qi_drain', 'soul_burn'
  ];
  
  const effect = effects[Math.floor(rng() * effects.length)];
  const duration = Math.floor(10 + rng() * 60 * level);  // 10 сек - 5 мин * level
  const qiCost = Math.floor(20 + level * 15);
  
  return {
    id,
    type: 'curse',
    subtype: 'combat',
    level,
    effect,
    duration,
    qiCost,
    reflectable: rng() > 0.3,
    resistanceStat: 'willpower',
    // ... остальные поля
  };
}
```

---

## 8️⃣ ПРИМЕРЫ ТЕХНИК

### 8.1 Защитные техники (Defense)

```typescript
// Энергетический щит
{
  id: "DF_000042",
  name: "Водяной барьер",
  type: "defense",
  subtype: "shield",
  element: "water",
  level: 2,
  qiCost: 25,
  effects: {
    shieldHP: 50,
    regeneration: 5,
    duration: 300,  // 5 минут
  }
}

// Активный блок
{
  id: "DF_000015",
  name: "Железная стена",
  type: "defense",
  subtype: "block",
  element: "earth",
  level: 3,
  qiCost: 30,
  effects: {
    damageReduction: 80,
    durability: 100,
    counterAttack: 10,
  }
}
```

### 8.2 Проклятия (Curse)

```typescript
// Боевое проклятие
{
  id: "CR_000008",
  name: "Печать молчания",
  type: "curse",
  subtype: "combat",
  element: "void",
  level: 3,
  qiCost: 40,
  effects: {
    effect: "silence",
    duration: 20,  // секунд
    reflectable: false,
    resistanceReduction: 0,
  }
}

// Ритуальное проклятие
{
  id: "CR_000100",
  name: "Проклятие бессилия",
  type: "curse",
  subtype: "ritual",
  element: "void",
  level: 6,
  qiCost: 500,
  effects: {
    effect: "cultivation_block",
    duration: { min: 0, max: 168 },  // до 7 дней
    reduction: 50,  // -50% к культивации
  },
  requirements: {
    ritualTime: 360,  // 6 часов
    ingredients: ["Кровь цели", "Костяная пыль"],
  }
}
```

### 8.3 Отравления (Poison)

```typescript
// Яд тела
{
  id: "PN_000025",
  name: "Кровавый корень",
  type: "poison",
  subtype: "body",
  element: "neutral",
  level: 3,
  delivery: "ingestion",
  stages: [
    { onset: 30, duration: 60, effects: { fatigue: 20 } },
    { onset: 90, duration: 120, effects: { hpDamage: 5, statReduction: { strength: 20 } } },
    { onset: 210, duration: 180, effects: { hpDamage: 15, paralysis: 30 } },
  ]
}

// Яд Ци
{
  id: "PN_000050",
  name: "Коррозия меридиан",
  type: "poison",
  subtype: "qi",
  element: "void",
  level: 5,
  delivery: "technique",
  effects: {
    qiCorruption: 5,  // 5% за ход
    meridianDamage: 10,
    conductivityReduction: 30,
  }
}
```

---

## 9️⃣ СВОДНАЯ ТАБЛИЦА ИЗМЕНЕНИЙ

| Категория | Было | Стало |
|-----------|------|-------|
| Защитные техники | type: "combat", combatType: "defense_*" | type: "defense", subtype: "shield/block/dodge" |
| Префикс защитных | TC | DF |
| Проклятия | Не определены | type: "curse", subtype: "combat/ritual" |
| Префикс проклятий | — | CR |
| Отравления | debuff: "poison" | type: "poison", subtype: "body/qi" |
| Префикс отравлений | — | PN |
| Слоты | combatSlots для всех | Отдельный curseSlot |

---

## 🔟 ДАЛЬНЕЙШИЕ ШАГИ

1. [ ] Обновить technique-generator.ts с новыми типами
2. [ ] Создать curse-generator.ts
3. [ ] Создать poison-generator.ts
4. [ ] Обновить id-system.md с новыми префиксами
5. [ ] Обновить UI для отображения новых типов
6. [ ] Добавить механики сопротивления

---

*Документ требует согласования перед интеграцией*
